Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('./_virtual/_rolldown/runtime.cjs');
const require_babel_plugin_intlayer_usage_analyzer = require('./babel-plugin-intlayer-usage-analyzer.cjs');
const require_extractScriptBlocks = require('./extractScriptBlocks.cjs');
const require_babel_plugin_intlayer_field_rename = require('./babel-plugin-intlayer-field-rename.cjs');
const require_transformers = require('./transformers.cjs');
let node_path = require("node:path");
let _intlayer_config_colors = require("@intlayer/config/colors");
_intlayer_config_colors = require_runtime.__toESM(_intlayer_config_colors);
let _intlayer_config_logger = require("@intlayer/config/logger");
let _intlayer_engine_utils = require("@intlayer/engine/utils");
let node_fs = require("node:fs");
let _babel_core = require("@babel/core");

//#region src/babel-plugin-intlayer-purge.ts
/**
* Cache of built {@link PruneContext} objects, keyed by the project's
* `baseDir`.  Each context is built exactly once per Node.js process:
* {@link runIntlayerPurgePipeline} registers the context before running so every
* subsequent transform is a no-op cache hit.
*/
const _pruneContextCache = /* @__PURE__ */ new Map();
/**
* Returns the shared {@link PruneContext} for the given base directory, or
* `null` if {@link intlayerPurgeBabelPlugin} has not yet been initialised for
* that directory.
*
* Used by {@link intlayerMinifyBabelPlugin} to read the rename map without
* creating a circular dependency.
*/
const getSharedPruneContext = (baseDir) => _pruneContextCache.get(baseDir) ?? null;
const isTranslationNode = (value) => typeof value === "object" && value !== null && value.nodeType === "translation" && typeof value.translation === "object";
const isPlainRecord = (value) => typeof value === "object" && value !== null && !Array.isArray(value);
/** Returns a copy of `record` keeping only the keys listed in `usedFieldNames`. */
const filterRecordByUsedFields = (record, usedFieldNames) => {
	const filteredRecord = {};
	for (const [fieldName, fieldValue] of Object.entries(record)) if (usedFieldNames.has(fieldName)) filteredRecord[fieldName] = fieldValue;
	return filteredRecord;
};
/**
* Removes unused fields from a **static** dictionary (all locales in one
* file). Supports shape A (translation node at the root) and shape B (flat
* record of translation nodes per field).
*/
const pruneStaticDictionaryContent = (dictionary, usedFieldNames) => {
	const { content } = dictionary;
	if (isTranslationNode(content)) {
		const firstLocaleValue = Object.values(content.translation)[0];
		if (isPlainRecord(firstLocaleValue)) {
			const prunedTranslation = {};
			for (const [locale, localeContent] of Object.entries(content.translation)) prunedTranslation[locale] = isPlainRecord(localeContent) ? filterRecordByUsedFields(localeContent, usedFieldNames) : localeContent;
			return {
				prunedDictionary: {
					...dictionary,
					content: {
						...content,
						translation: prunedTranslation
					}
				},
				wasRecognised: true
			};
		}
	}
	if (isPlainRecord(content) && !isTranslationNode(content)) return {
		prunedDictionary: {
			...dictionary,
			content: filterRecordByUsedFields(content, usedFieldNames)
		},
		wasRecognised: true
	};
	return {
		prunedDictionary: dictionary,
		wasRecognised: false
	};
};
/**
* Removes unused fields from a **dynamic / per-locale** dictionary file
* (one JSON per locale, flat `content` record).
*/
const pruneDynamicDictionaryContent = (dictionary, usedFieldNames) => {
	const { content } = dictionary;
	if (!isPlainRecord(content)) return {
		prunedDictionary: dictionary,
		wasRecognised: false
	};
	return {
		prunedDictionary: {
			...dictionary,
			content: filterRecordByUsedFields(content, usedFieldNames)
		},
		wasRecognised: true
	};
};
/**
* Recursively renames user-defined content fields using `renameMap`.
* Translation nodes, arrays, and primitives follow the same traversal rules
* as in the Vite-based minify plugin.
*/
const renameContentRecursively = (value, renameMap) => {
	if (Array.isArray(value)) return value.map((element) => renameContentRecursively(element, renameMap));
	if (!value || typeof value !== "object") return value;
	const record = value;
	if (typeof record.nodeType === "string" && record.translation && typeof record.translation === "object" && !Array.isArray(record.translation)) {
		const renamedTranslation = {};
		for (const [locale, localeValue] of Object.entries(record.translation)) renamedTranslation[locale] = renameContentRecursively(localeValue, renameMap);
		return {
			...record,
			translation: renamedTranslation
		};
	}
	const result = {};
	for (const [key, val] of Object.entries(record)) {
		const renameEntry = renameMap.get(key);
		if (renameEntry) result[renameEntry.shortName] = renameContentRecursively(val, renameEntry.children);
		else result[key] = val;
	}
	return result;
};
/**
* Applies a {@link NestedRenameMap} to a parsed dictionary object, renaming
* only the keys inside `content` while leaving top-level metadata untouched.
*/
const applyFieldRenameToDict = (dict, renameMap) => {
	const content = dict.content;
	if (!content || typeof content !== "object" || Array.isArray(content)) return dict;
	return {
		...dict,
		content: renameContentRecursively(content, renameMap)
	};
};
/**
* Runs the usage-analyser Babel plugin synchronously on a single code block,
* accumulating results into `pruneContext`.
*/
const analyzeCodeBlockSync = (code, sourceFilePath, pruneContext, compatCallers) => {
	try {
		(0, _babel_core.transformSync)(code, {
			filename: sourceFilePath,
			plugins: [require_babel_plugin_intlayer_usage_analyzer.makeUsageAnalyzerBabelPlugin(pruneContext, { compatCallers })],
			parserOpts: require_transformers.BABEL_PARSER_OPTIONS,
			ast: false,
			code: false
		});
	} catch {
		pruneContext.hasUnparsableSourceFiles = true;
	}
};
/**
* Reads a source file from disk and runs the usage-analyser synchronously.
* SFC files (Vue / Svelte) are handled by extracting script blocks first.
*/
const analyzeSourceFileSync = (sourceFilePath, pruneContext, compatCallers) => {
	let code;
	try {
		code = (0, node_fs.readFileSync)(sourceFilePath, "utf-8");
	} catch {
		return;
	}
	const usageCheckRegex = require_transformers.getUsageCheckRegex(compatCallers);
	if (!usageCheckRegex.test(code)) return;
	const scriptBlocks = require_extractScriptBlocks.extractScriptBlocks(sourceFilePath, code);
	for (const block of scriptBlocks) {
		if (!usageCheckRegex.test(block.content)) continue;
		analyzeCodeBlockSync(block.content, sourceFilePath, pruneContext, compatCallers);
	}
};
/**
* Reads compiled dictionary JSON files to build the nested field-rename maps,
* mirroring the Phase 4 logic in the Vite `intlayerOptimize` plugin's
* `buildStart` hook.  Results are stored in
* `pruneContext.dictionaryKeyToFieldRenameMap`.
*/
const buildRenameMapsSynchronously = (dictionariesDir, dynamicDictionariesDir, dictionaryKeyToImportModeMap, pruneContext, logger) => {
	let partiallyMinifiedDictionariesCount = 0;
	for (const [dictionaryKey, fieldUsage] of pruneContext.dictionaryKeyToFieldUsageMap) {
		if (fieldUsage === "all") continue;
		if (dictionaryKeyToImportModeMap[dictionaryKey] === "fetch") continue;
		if (pruneContext.dictionariesSkippingFieldRename.has(dictionaryKey)) continue;
		let dictionaryContent = null;
		const staticJsonPath = (0, node_path.join)(dictionariesDir, `${dictionaryKey}.json`);
		if ((0, node_fs.existsSync)(staticJsonPath)) try {
			const raw = (0, node_fs.readFileSync)(staticJsonPath, "utf-8");
			dictionaryContent = JSON.parse(raw).content;
		} catch {}
		if (!dictionaryContent) {
			const dynamicDirPath = (0, node_path.join)(dynamicDictionariesDir, dictionaryKey);
			if ((0, node_fs.existsSync)(dynamicDirPath)) try {
				const firstJsonFile = (0, node_fs.readdirSync)(dynamicDirPath).find((file) => file.endsWith(".json"));
				if (firstJsonFile) {
					const raw = (0, node_fs.readFileSync)((0, node_path.join)(dynamicDirPath, firstJsonFile), "utf-8");
					dictionaryContent = JSON.parse(raw).content;
				}
			} catch {}
		}
		if (!dictionaryContent) continue;
		const nestedRenameMap = require_babel_plugin_intlayer_field_rename.buildNestedRenameMapFromContent(dictionaryContent);
		const opaqueFieldMap = pruneContext.dictionaryKeysWithOpaqueFields.get(dictionaryKey);
		if (opaqueFieldMap) {
			const dangerousOccurrences = [...opaqueFieldMap.values()].filter((occurrence) => (require_babel_plugin_intlayer_field_rename.getNestedRenameEntryAtPath(nestedRenameMap, occurrence.fieldPath)?.children.size ?? 0) > 0);
			if (dangerousOccurrences.length > 0) {
				partiallyMinifiedDictionariesCount += 1;
				logger([
					`Dictionary`,
					(0, _intlayer_config_logger.colorizeKey)(dictionaryKey),
					`partially minified.`,
					...dangerousOccurrences.flatMap((occurrence) => [
						`\n    Opaque field:`,
						(0, _intlayer_config_logger.colorize)(`'${occurrence.fieldPath.join(".")}'`, _intlayer_config_colors.BLUE),
						`(nested keys preserved for stability).`,
						...occurrence.locations.map((location) => `\n      at ${(0, _intlayer_engine_utils.formatPath)(location)}`)
					])
				], {
					level: "warn",
					isVerbose: true
				});
				for (const occurrence of dangerousOccurrences) {
					const renameEntry = require_babel_plugin_intlayer_field_rename.getNestedRenameEntryAtPath(nestedRenameMap, occurrence.fieldPath);
					if (renameEntry) renameEntry.children = /* @__PURE__ */ new Map();
				}
			}
		}
		if (nestedRenameMap.size > 0) pruneContext.dictionaryKeyToFieldRenameMap.set(dictionaryKey, nestedRenameMap);
	}
	if (partiallyMinifiedDictionariesCount > 0) logger([
		`Partially minified`,
		(0, _intlayer_config_logger.colorizeNumber)(partiallyMinifiedDictionariesCount),
		`dictionar${partiallyMinifiedDictionariesCount === 1 ? "y" : "ies"}`,
		`(preserved nested keys for opaque fields).`
	]);
};
/**
* Names of the top-level content fields of a compiled dictionary, whatever
* shape its `content` uses.  Used to report which fields the purge removed.
*/
const readContentFieldNames = (content) => {
	if (isTranslationNode(content)) {
		const firstLocaleValue = Object.values(content.translation)[0];
		return isPlainRecord(firstLocaleValue) ? Object.keys(firstLocaleValue) : [];
	}
	if (isPlainRecord(content)) return Object.keys(content);
	return [];
};
/**
* Reads a compiled dictionary JSON file, applies the purge and/or minify
* transformations, and writes it back in-place when something changed.
*
* `dictionaryKind` selects the prune strategy and the minified output shape:
* static dictionaries keep `{ key, content }`, dynamic per-locale
* dictionaries additionally keep `locale`.
*/
const processDictionaryFile = (filePath, dictionaryKind, pruneContext, shouldPurge, shouldMinify, logger, stats) => {
	let parsedDict;
	try {
		parsedDict = JSON.parse((0, node_fs.readFileSync)(filePath, "utf-8"));
	} catch {
		return;
	}
	const { key: dictionaryKey } = parsedDict;
	if (!dictionaryKey) {
		logger([
			`Dictionary file`,
			(0, _intlayer_engine_utils.formatPath)(filePath),
			`is missing a "key" field. Skipping prune for this file.`
		], { level: "warn" });
		return;
	}
	if (pruneContext.dictionariesWithEdgeCases.has(dictionaryKey)) return;
	let modified = false;
	if (shouldPurge) {
		const fieldUsage = pruneContext.dictionaryKeyToFieldUsageMap.get(dictionaryKey);
		if (fieldUsage && fieldUsage !== "all") {
			const pruneDictionaryContent = dictionaryKind === "static" ? pruneStaticDictionaryContent : pruneDynamicDictionaryContent;
			const originalFieldNames = readContentFieldNames(parsedDict.content);
			const { prunedDictionary, wasRecognised } = pruneDictionaryContent(parsedDict, fieldUsage);
			if (!wasRecognised) {
				pruneContext.dictionariesWithEdgeCases.add(dictionaryKey);
				logger([
					`Unrecognised content structure in dictionary`,
					(0, _intlayer_config_logger.colorizeKey)(dictionaryKey),
					`(file:`,
					`${(0, _intlayer_engine_utils.formatPath)(filePath)}).`,
					`Skipping prune for this dictionary.`
				], { level: "warn" });
				return;
			}
			const removedFieldNames = originalFieldNames.filter((fieldName) => !fieldUsage.has(fieldName));
			if (removedFieldNames.length > 0) {
				stats.prunedFieldsCountPerDictionary.set(dictionaryKey, removedFieldNames.length);
				if (!stats.loggedPrunedDictionaryKeys.has(dictionaryKey)) {
					stats.loggedPrunedDictionaryKeys.add(dictionaryKey);
					logger([
						`Pruned`,
						(0, _intlayer_config_logger.colorizeNumber)(removedFieldNames.length),
						`unused field${removedFieldNames.length === 1 ? "" : "s"} from`,
						`${(0, _intlayer_config_logger.colorizeKey)(dictionaryKey)}:`,
						removedFieldNames.map((fieldName) => (0, _intlayer_config_logger.colorize)(fieldName, _intlayer_config_colors.GREY_LIGHT)).join(", ")
					], { isVerbose: true });
				}
			}
			parsedDict = prunedDictionary;
			modified = true;
		} else if (!fieldUsage && pruneContext.hasUnparsableSourceFiles && !stats.loggedIncompleteAnalysisKeys.has(dictionaryKey)) {
			stats.loggedIncompleteAnalysisKeys.add(dictionaryKey);
			logger([
				`Skipping prune for dictionary`,
				(0, _intlayer_config_logger.colorizeKey)(dictionaryKey),
				`: analysis is incomplete due to earlier source-file parse failures.`
			], { level: "warn" });
		}
	}
	if (shouldMinify) {
		const fieldRenameMap = pruneContext.dictionaryKeyToFieldRenameMap.get(dictionaryKey);
		if (fieldRenameMap && fieldRenameMap.size > 0) {
			parsedDict = applyFieldRenameToDict(parsedDict, fieldRenameMap);
			modified = true;
		}
	}
	if (!modified) return;
	const outputDict = shouldMinify ? dictionaryKind === "static" ? {
		key: parsedDict.key,
		content: parsedDict.content
	} : {
		key: parsedDict.key,
		content: parsedDict.content,
		locale: parsedDict.locale
	} : parsedDict;
	try {
		(0, node_fs.writeFileSync)(filePath, JSON.stringify(outputDict), "utf-8");
	} catch {}
};
const processAllDictionaryFiles = (dictionariesDir, dynamicDictionariesDir, pruneContext, shouldPurge, shouldMinify, logger) => {
	const stats = {
		prunedFieldsCountPerDictionary: /* @__PURE__ */ new Map(),
		loggedPrunedDictionaryKeys: /* @__PURE__ */ new Set(),
		loggedIncompleteAnalysisKeys: /* @__PURE__ */ new Set()
	};
	if ((0, node_fs.existsSync)(dictionariesDir)) for (const entry of (0, node_fs.readdirSync)(dictionariesDir)) {
		if (!entry.endsWith(".json")) continue;
		processDictionaryFile((0, node_path.join)(dictionariesDir, entry), "static", pruneContext, shouldPurge, shouldMinify, logger, stats);
	}
	if ((0, node_fs.existsSync)(dynamicDictionariesDir)) for (const keyDir of (0, node_fs.readdirSync)(dynamicDictionariesDir)) {
		const keyDirPath = (0, node_path.join)(dynamicDictionariesDir, keyDir);
		try {
			for (const localeFile of (0, node_fs.readdirSync)(keyDirPath)) {
				if (!localeFile.endsWith(".json")) continue;
				processDictionaryFile((0, node_path.join)(keyDirPath, localeFile), "dynamic", pruneContext, shouldPurge, shouldMinify, logger, stats);
			}
		} catch {}
	}
	const totalPrunedFieldsCount = [...stats.prunedFieldsCountPerDictionary.values()].reduce((total, count) => total + count, 0);
	const totalPrunedDictionariesCount = stats.prunedFieldsCountPerDictionary.size;
	if (totalPrunedFieldsCount > 0) logger([
		`Pruned`,
		(0, _intlayer_config_logger.colorizeNumber)(totalPrunedFieldsCount),
		`unused field${totalPrunedFieldsCount === 1 ? "" : "s"} across`,
		(0, _intlayer_config_logger.colorizeNumber)(totalPrunedDictionariesCount),
		`dictionar${totalPrunedDictionariesCount === 1 ? "y" : "ies"}.`
	]);
};
/**
* Explains why the requested steps did nothing: the visual editor needs the
* full dictionary content, so both purge and minify stand down when it is on.
*/
const logStepDisabledByEditor = (logger, purge, minify) => {
	const explainDisabled = (stepLabel) => logger([
		stepLabel,
		"is",
		(0, _intlayer_config_logger.colorize)("disabled", _intlayer_config_colors.GREY_DARK),
		"because",
		(0, _intlayer_config_logger.colorize)("editor.enabled", _intlayer_config_colors.BLUE),
		"is",
		(0, _intlayer_config_logger.colorize)("true", _intlayer_config_colors.GREY_DARK),
		(0, _intlayer_config_logger.colorize)("— the editor requires full dictionary content", _intlayer_config_colors.GREY)
	]);
	if (purge) explainDisabled("Dictionary purge");
	if (minify) explainDisabled("Dictionary minification");
};
/**
* Reports the dictionaries whose consumption could not be tracked statically,
* with the source files responsible — those keep their full content.
*/
const logUntrackedBindings = (logger, pruneContext) => {
	for (const [dictionaryKey, sourceFilePaths] of pruneContext.dictionaryKeysWithUntrackedBindings) logger([
		`Dictionary`,
		(0, _intlayer_config_logger.colorizeKey)(dictionaryKey),
		`cannot be purged or minified.`,
		`\n    Reason: the result of`,
		`${(0, _intlayer_config_logger.colorize)(`useIntlayer(`, _intlayer_config_colors.GREY_LIGHT)}${(0, _intlayer_config_logger.colorizeKey)(`'${dictionaryKey}'`)}${(0, _intlayer_config_logger.colorize)(`)`, _intlayer_config_colors.GREY_LIGHT)}`,
		`is assigned to a plain variable in:`,
		...sourceFilePaths.map((filePath) => `\n      - ${(0, _intlayer_engine_utils.formatPath)(filePath)}`)
	], { level: "warn" });
};
/**
* Runs the full purge/minify pipeline for the given options, using a
* module-level cache so the work happens at most once per process per
* unique `baseDir`.
*
* Exported so bundler integrations that do not go through Babel can drive the
* same pipeline. The Next.js integration is the main consumer: its SWC plugin
* runs as a per-file Wasm transform with no filesystem access, so the analysis
* and the dictionary rewrite happen here, in Node, and only the resulting
* field-rename tables (see {@link serializeFieldRenameMap}) are forwarded to
* the plugin.
*
* **This performs file I/O**: it reads every source file listed in
* `componentFilesList` and overwrites the compiled dictionary JSON files
* in-place. Call it at most once per build, after the dictionaries have been
* built.
*/
const runIntlayerPurgePipeline = (options) => {
	const { baseDir, purge, minify, optimize, editorEnabled, dictionariesDir, dynamicDictionariesDir, componentFilesList, dictionaryKeyToImportModeMap, nestedDictionaryReferences, compatCallers, logConfig } = options;
	const cachedContext = _pruneContextCache.get(baseDir);
	if (cachedContext) return cachedContext;
	const pruneContext = require_babel_plugin_intlayer_usage_analyzer.createPruneContext();
	_pruneContextCache.set(baseDir, pruneContext);
	const logger = (0, _intlayer_config_logger.getAppLogger)(logConfig);
	const shouldPurge = purge && !editorEnabled;
	const shouldMinify = minify && !editorEnabled;
	if (editorEnabled && (purge || minify)) logStepDisabledByEditor(logger, purge, minify);
	if (!shouldPurge && !shouldMinify || optimize === false) return pruneContext;
	for (const sourceFilePath of componentFilesList) {
		if (!require_transformers.SOURCE_FILE_REGEX.test(sourceFilePath)) continue;
		analyzeSourceFileSync(sourceFilePath, pruneContext, compatCallers);
	}
	if (nestedDictionaryReferences) require_babel_plugin_intlayer_usage_analyzer.preserveNestedDictionaryFields(pruneContext, nestedDictionaryReferences);
	logUntrackedBindings(logger, pruneContext);
	if (shouldMinify) buildRenameMapsSynchronously(dictionariesDir, dynamicDictionariesDir, dictionaryKeyToImportModeMap, pruneContext, logger);
	processAllDictionaryFiles(dictionariesDir, dynamicDictionariesDir, pruneContext, shouldPurge, shouldMinify, logger);
	return pruneContext;
};
/**
* Babel plugin that analyses all project source files and rewrites compiled
* dictionary JSON files in-place to remove unused content fields
* (`build.purge`) and/or rename them to short alphabetic aliases
* (`build.minify`).
*
* All option values must be pre-resolved via {@link getPurgePluginOptions}
* before being passed here — the plugin does not load the intlayer
* configuration itself.
*
* This plugin performs **file I/O as a side effect**: on the very first Babel
* transform in a given Node.js process it synchronously scans the component
* files listed in `options.componentFilesList`, builds field-usage data, and
* writes the processed dictionaries to disk.  Subsequent transforms are
* no-ops.
*
* Source-code field renames (rewriting `content.title` → `content.a`) are
* handled by the companion {@link intlayerMinifyBabelPlugin}.
*
* @example
* ```js
* // babel.config.js
* const {
*   intlayerPurgeBabelPlugin,
*   intlayerMinifyBabelPlugin,
*   intlayerOptimizeBabelPlugin,
*   getPurgePluginOptions,
*   getMinifyPluginOptions,
*   getOptimizePluginOptions,
* } = require("@intlayer/babel");
*
* module.exports = {
*   presets: ["next/babel"],
*   plugins: [
*     [intlayerPurgeBabelPlugin,    getPurgePluginOptions()],
*     [intlayerMinifyBabelPlugin,   getMinifyPluginOptions()],
*     [intlayerOptimizeBabelPlugin, getOptimizePluginOptions()],
*   ],
* };
* ```
*
* @remarks
* - Intended for **production builds** only.  Dictionary JSON files are
*   overwritten in-place; running `intlayer build` afterwards restores the
*   originals.
* - The plugin is a no-op when `optimize` is `false` or `editorEnabled` is
*   `true`.
*/
const intlayerPurgeBabelPlugin = (_babel) => ({
	name: "intlayer-purge",
	pre() {
		runIntlayerPurgePipeline(this.opts);
	},
	visitor: {}
});

//#endregion
exports.getSharedPruneContext = getSharedPruneContext;
exports.intlayerPurgeBabelPlugin = intlayerPurgeBabelPlugin;
exports.runIntlayerPurgePipeline = runIntlayerPurgePipeline;
//# sourceMappingURL=babel-plugin-intlayer-purge.cjs.map