import { ENUMERATION, TypedNodeModel } from "@intlayer/types/nodeType";
//#region src/transpiler/enumeration/enumeration.d.ts
type Positive = number | `${number}`;
type Negative = `-${number}`;
type Numbers = Positive | Negative;
type Equal = Numbers;
type EqualString = `=${Numbers}`;
type Superior = `>${Numbers}`;
type SuperiorOrEqual = `>=${Numbers}`;
type Inferior = `<${Numbers}`;
type InferiorOrEqual = `<=${Numbers}`;
type EnterFormat = Equal | EqualString | Superior | SuperiorOrEqual | Inferior | InferiorOrEqual;
type EnumerationContentState<Content> = Partial<Record<EnterFormat, Content>> & {
  fallback?: Content;
};
type EnumerationContent<Content = unknown> = TypedNodeModel<typeof ENUMERATION, EnumerationContentState<Content>>;
/**
 * Function intended to be used to build intlayer dictionaries.
 *
 * Allow to pick a content based on a quantity.
 *
 * Usage:
 *
 * ```ts
 * enu({
 *  '<=-2.3': 'You have less than -2.3',
 *  '<1': 'You have less than one',
 *  '2': 'You have two',
 *  '>=3': 'You have three or more',
 * });
 * ```
 *
 * > The order of the keys will define the priority of the content.
 *
 */
declare const enumeration: <Content = unknown>(content?: EnumerationContentState<Content>) => EnumerationContent<Content>;
//#endregion
export { EnterFormat, EnumerationContent, EnumerationContentState, enumeration as enu };
//# sourceMappingURL=enumeration.d.ts.map