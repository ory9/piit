Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
const require_init_utils_configManipulation = require('./utils/configManipulation.cjs');
const require_init_utils_fileSystem = require('./utils/fileSystem.cjs');
const require_init_cms = require('./cms.cjs');
const require_initConfig_index = require('../initConfig/index.cjs');
const require_init_utils_version = require('./utils/version.cjs');
const require_init_frameworkSetup_index = require('./frameworkSetup/index.cjs');
const require_init_utils_packageManager = require('./utils/packageManager.cjs');
const require_init_utils_githubActions = require('./utils/githubActions.cjs');
const require_init_utils_jsonParser = require('./utils/jsonParser.cjs');
const require_init_utils_nextCompilerBabel = require('./utils/nextCompilerBabel.cjs');
const require_init_utils_tsConfig = require('./utils/tsConfig.cjs');
let node_path = require("node:path");
let _intlayer_config_logger = require("@intlayer/config/logger");
let _intlayer_config_node = require("@intlayer/config/node");
let _intlayer_config_utils = require("@intlayer/config/utils");
let _intlayer_config_colors = require("@intlayer/config/colors");
_intlayer_config_colors = require_runtime.__toESM(_intlayer_config_colors);

//#region src/init/index.ts
/**
* Documentation URL Constants
*/
const DocumentationRouter = {
	NextJS: "https://intlayer.org/doc/environment/nextjs.md",
	NextJS_15: "https://intlayer.org/doc/environment/nextjs/15.md",
	NextJS_14: "https://intlayer.org/doc/environment/nextjs/14.md",
	CRA: "https://intlayer.org/doc/environment/create-react-app.md",
	Astro: "https://intlayer.org/doc/environment/astro.md",
	ViteAndReact: "https://intlayer.org/doc/environment/vite-and-react.md",
	ViteAndReact_ReactRouterV7: "https://intlayer.org/doc/environment/vite-and-react/react-router-v7.md",
	ViteAndReact_ReactRouterV7_FSRoutes: "https://intlayer.org/doc/environment/vite-and-react/react-router-v7-fs-routes.md",
	ViteAndVue: "https://intlayer.org/doc/environment/vite-and-vue.md",
	ViteAndSolid: "https://intlayer.org/doc/environment/vite-and-solid.md",
	ViteAndSvelte: "https://intlayer.org/doc/environment/vite-and-svelte.md",
	ViteAndPreact: "https://intlayer.org/doc/environment/vite-and-preact.md",
	TanStackRouter: "https://intlayer.org/doc/environment/tanstack-start.md",
	NuxtAndVue: "https://intlayer.org/doc/environment/nuxt-and-vue.md",
	Angular: "https://intlayer.org/doc/environment/angular.md",
	SvelteKit: "https://intlayer.org/doc/environment/sveltekit.md",
	ReactNativeAndExpo: "https://intlayer.org/doc/environment/react-native-and-expo.md",
	Lynx: "https://intlayer.org/doc/environment/lynx-and-react.md",
	Express: "https://intlayer.org/doc/environment/express.md",
	NestJS: "https://intlayer.org/doc/environment/nest.md",
	Fastify: "https://intlayer.org/doc/environment/fastify.md",
	Default: "https://intlayer.org/doc/get-started",
	LSP: "https://intlayer.org/doc/lsp.md",
	ESLint: "https://intlayer.org/doc/eslint.md",
	NextIntl: "https://intlayer.org/blog/intlayer-with-next-intl.md",
	ReactI18Next: "https://intlayer.org/blog/intlayer-with-react-i18next.md",
	ReactIntl: "https://intlayer.org/blog/intlayer-with-react-intl.md",
	NextI18Next: "https://intlayer.org/blog/intlayer-with-next-i18next.md",
	VueI18n: "https://intlayer.org/blog/intlayer-with-vue-i18n.md"
};
/**
* The Intlayer lint setup, as `intlayer init` writes it into `.oxlintrc.json`.
*
* Declared locally on purpose: `@intlayer/engine` is installed by every Intlayer
* user, so it must not depend on a lint plugin to write a handful of strings
* into a JSON file. oxlint resolves a plugin's *rules* but not its shipped
* *configs*, so the preset cannot be referenced by name there either.
*
* Mirrors the `recommended` preset of `eslint-plugin-intlayer`; keep in sync
* when a rule is enabled by default there.
*/
const LINT_PLUGIN_PACKAGE_NAME = "eslint-plugin-intlayer";
/** Intlayer's generated output — never source, never worth linting. */
const GENERATED_OUTPUT_GLOBS = ["**/.intlayer/**"];
/** Rules of the `recommended` preset, prefixed by the plugin namespace. */
const getRecommendedRules = () => ({
	"intlayer/no-raw-text": "warn",
	"intlayer/static-dictionary-key": "error",
	"intlayer/no-dynamic-field-access": "error"
});
/**
* Helper: Detects the environment and returns the doc URL
*/
const getDocumentationUrl = (packageJson) => {
	const deps = {
		...packageJson.dependencies,
		...packageJson.devDependencies
	};
	/**
	* Helper to check if a version string matches a specific major version
	* Matches: "15", "^15.0.0", "~15.2", "15.0.0-beta"
	*/
	const isVersion = (versionString, major) => {
		if (!versionString || typeof versionString !== "string") return false;
		return new RegExp(`^[\\^~]?${major}(?:\\.|$)`).test(versionString);
	};
	if (deps["@lynx-js/react"] || deps["@lynx-js/core"]) return DocumentationRouter.Lynx;
	if (deps["react-native"] || deps.expo) return DocumentationRouter.ReactNativeAndExpo;
	if (deps.next) {
		const version = deps.next;
		if (isVersion(version, 14)) return DocumentationRouter.NextJS_14;
		if (isVersion(version, 15)) return DocumentationRouter.NextJS_15;
		return DocumentationRouter.NextJS;
	}
	if (deps.nuxt) return DocumentationRouter.NuxtAndVue;
	if (deps.astro) return DocumentationRouter.Astro;
	if (deps["@sveltejs/kit"]) return DocumentationRouter.SvelteKit;
	if (deps["@tanstack/react-router"]) return DocumentationRouter.TanStackRouter;
	const reactRouterVersion = deps["react-router"];
	if (reactRouterVersion && typeof reactRouterVersion === "string") {
		if (deps["@react-router/fs-routes"]) return DocumentationRouter.ViteAndReact_ReactRouterV7_FSRoutes;
		if (isVersion(reactRouterVersion, 7)) return DocumentationRouter.ViteAndReact_ReactRouterV7;
	}
	if (deps.vite) {
		if (deps.vue) return DocumentationRouter.ViteAndVue;
		if (deps["solid-js"]) return DocumentationRouter.ViteAndSolid;
		if (deps.svelte) return DocumentationRouter.ViteAndSvelte;
		if (deps.preact) return DocumentationRouter.ViteAndPreact;
		return DocumentationRouter.ViteAndReact;
	}
	if (deps["react-scripts"]) return DocumentationRouter.CRA;
	if (deps["@angular/core"]) return DocumentationRouter.Angular;
	if (deps["@nestjs/core"]) return DocumentationRouter.NestJS;
	if (deps.express) return DocumentationRouter.Express;
	if (deps.fastify) return DocumentationRouter.Fastify;
	if (deps["next-intl"]) return DocumentationRouter.NextIntl;
	if (deps["react-i18next"] || deps.i18next) return DocumentationRouter.ReactI18Next;
	if (deps["react-intl"]) return DocumentationRouter.ReactIntl;
	if (deps["next-i18next"]) return DocumentationRouter.NextI18Next;
	if (deps["vue-i18n"]) return DocumentationRouter.VueI18n;
	return DocumentationRouter.Default;
};
/**
* Resolves the default dev-server URL of the detected framework, used as the
* `editor.applicationURL` of the generated configuration file. Frameworks that
* embed Vite (Astro, SvelteKit, …) are checked before the bare Vite fallback so
* their own dev port wins.
*/
const getDefaultApplicationURL = (dependencies) => {
	if (dependencies.next) return "http://localhost:3000";
	if (dependencies.astro) return "http://localhost:4321";
	if (dependencies["@angular/core"]) return "http://localhost:4200";
	if (dependencies.nuxt) return "http://localhost:3000";
	if (dependencies["@sveltejs/kit"]) return "http://localhost:5173";
	if (dependencies["react-scripts"]) return "http://localhost:3000";
	if (dependencies.vite) return "http://localhost:5173";
	return "http://localhost:3000";
};
/**
* MAIN LOGIC
*/
const initIntlayer = async (rootDir, options) => {
	(0, _intlayer_config_logger.logger)((0, _intlayer_config_logger.colorize)("Checking Intlayer configuration...", _intlayer_config_colors.CYAN));
	const packageJsonPath = "package.json";
	if (!await require_init_utils_fileSystem.exists(rootDir, packageJsonPath)) {
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} No ${(0, _intlayer_config_logger.colorizePath)("package.json")} found. Please run this script from the project root.`, { level: "error" });
		process.exit(1);
	}
	const packageJsonContent = await require_init_utils_fileSystem.readFileFromRoot(rootDir, packageJsonPath);
	let packageJson;
	try {
		packageJson = JSON.parse(packageJsonContent);
	} catch {
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not parse ${(0, _intlayer_config_logger.colorizePath)("package.json")}.`, { level: "error" });
		process.exit(1);
	}
	const guideUrl = getDocumentationUrl(packageJson);
	const allDeps = {
		...packageJson.dependencies ?? {},
		...packageJson.devDependencies ?? {}
	};
	const packageManager = require_init_utils_packageManager.detectPackageManager(rootDir);
	const linguiPresent = Boolean(allDeps["@lingui/core"] || allDeps["@lingui/react"] || allDeps["@intlayer/lingui"]);
	const linguiCatalog = linguiPresent ? await require_init_utils_fileSystem.detectLinguiCatalogPattern(rootDir) : null;
	const linguiCatalogFormat = linguiCatalog?.format ?? (linguiPresent ? "po" : null);
	const { packagesToInstall, devPackagesToInstall, compatSyncConfig, compatVitePluginConfig } = require_init_utils_packageManager.detectMissingIntlayerPackages(allDeps, { linguiCatalogFormat });
	const wantsJsonNamespaces = options?.contentStrategy === "json-namespaces" && !compatSyncConfig;
	if (wantsJsonNamespaces && !allDeps["@intlayer/sync-json-plugin"] && !devPackagesToInstall.includes("@intlayer/sync-json-plugin")) devPackagesToInstall.push("@intlayer/sync-json-plugin");
	if (!options?.noInstallPackages) {
		const withVersion = (packages) => options?.upgradeToVersion ? packages.map((pkg) => `${pkg}@${options.upgradeToVersion}`) : packages;
		if (packagesToInstall.length > 0) {
			(0, _intlayer_config_logger.logger)((0, _intlayer_config_logger.colorize)("Installing missing Intlayer dependencies...", _intlayer_config_colors.CYAN));
			try {
				require_init_utils_packageManager.installPackages(rootDir, withVersion(packagesToInstall), packageManager);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Installed: ${packagesToInstall.map((pkg) => (0, _intlayer_config_logger.colorize)(pkg, _intlayer_config_colors.MAGENTA)).join(", ")}`);
			} catch {
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Failed to install packages. Please install manually: ${packagesToInstall.join(" ")}`, { level: "warn" });
			}
		}
		if (devPackagesToInstall.length > 0) {
			(0, _intlayer_config_logger.logger)((0, _intlayer_config_logger.colorize)("Installing missing Intlayer dev dependencies...", _intlayer_config_colors.CYAN));
			try {
				require_init_utils_packageManager.installPackages(rootDir, withVersion(devPackagesToInstall), packageManager, true);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Installed: ${devPackagesToInstall.map((pkg) => (0, _intlayer_config_logger.colorize)(pkg, _intlayer_config_colors.MAGENTA)).join(", ")}`);
			} catch {
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Failed to install dev packages. Please install manually: ${devPackagesToInstall.join(" ")}`, { level: "warn" });
			}
		}
		if (options?.upgradeToVersion) {
			const outdatedDependencies = require_init_utils_packageManager.detectOutdatedIntlayerPackages(rootDir, packageJson.dependencies ?? {}, options.upgradeToVersion);
			const outdatedDevDependencies = require_init_utils_packageManager.detectOutdatedIntlayerPackages(rootDir, packageJson.devDependencies ?? {}, options.upgradeToVersion);
			const allOutdated = [...outdatedDependencies, ...outdatedDevDependencies];
			if (allOutdated.length > 0) {
				(0, _intlayer_config_logger.logger)((0, _intlayer_config_logger.colorize)(`Upgrading outdated Intlayer dependencies to ${options.upgradeToVersion}...`, _intlayer_config_colors.CYAN));
				try {
					require_init_utils_packageManager.upgradePackages(rootDir, outdatedDependencies, packageManager, options.upgradeToVersion);
					require_init_utils_packageManager.upgradePackages(rootDir, outdatedDevDependencies, packageManager, options.upgradeToVersion, true);
					(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Upgraded: ${allOutdated.map((pkg) => (0, _intlayer_config_logger.colorize)(pkg, _intlayer_config_colors.MAGENTA)).join(", ")}`);
				} catch {
					(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Failed to upgrade packages. Please upgrade manually: ${allOutdated.join(" ")}`, { level: "warn" });
				}
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Intlayer dependencies are up to date`);
		}
	}
	const gitignorePath = ".gitignore";
	if (!options?.noGitignore && await require_init_utils_fileSystem.exists(rootDir, gitignorePath)) {
		const gitignoreContent = await require_init_utils_fileSystem.readFileFromRoot(rootDir, gitignorePath);
		if (!gitignoreContent.includes("intlayer")) {
			const newContent = `${gitignoreContent}\n# Intlayer\n.intlayer\n`;
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, gitignorePath, newContent);
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Added ${(0, _intlayer_config_logger.colorizePath)(".intlayer")} to ${(0, _intlayer_config_logger.colorizePath)(gitignorePath)}`);
		} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(gitignorePath)} already includes .intlayer`);
	}
	if (!options?.noGithubActions) {
		const workflowsContext = await require_init_utils_githubActions.resolveGithubWorkflowsContext(rootDir, packageManager);
		const workflows = require_init_utils_githubActions.getGithubWorkflows(workflowsContext.packageManager, workflowsContext.options);
		for (const workflow of workflows) {
			if (await require_init_utils_fileSystem.exists(workflowsContext.workflowsRootDir, workflow.filePath)) {
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(workflow.filePath)} already exists`);
				continue;
			}
			try {
				await require_init_utils_fileSystem.ensureDirectory(workflowsContext.workflowsRootDir, (0, node_path.join)(".github", "workflows"));
				await require_init_utils_fileSystem.writeFileToRoot(workflowsContext.workflowsRootDir, workflow.filePath, workflow.content);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Added GitHub Actions workflow ${(0, _intlayer_config_logger.colorizePath)(workflow.filePath)}`);
			} catch {
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not create ${(0, _intlayer_config_logger.colorizePath)(workflow.filePath)}. You may need to add it manually.`, { level: "warn" });
			}
		}
	}
	const vscodeDir = ".vscode";
	const extensionsJsonPath = (0, node_path.join)(vscodeDir, "extensions.json");
	const extensionId = "intlayer.intlayer-vs-code-extension";
	if (!options?.noVscodeExtension) try {
		let extensionsConfig = { recommendations: [] };
		if (await require_init_utils_fileSystem.exists(rootDir, extensionsJsonPath)) {
			const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, extensionsJsonPath);
			extensionsConfig = require_init_utils_jsonParser.parseJSONWithComments(content);
		} else await require_init_utils_fileSystem.ensureDirectory(rootDir, vscodeDir);
		if (!extensionsConfig.recommendations) extensionsConfig.recommendations = [];
		if (!extensionsConfig.recommendations.includes(extensionId)) {
			extensionsConfig.recommendations.push(extensionId);
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, extensionsJsonPath, JSON.stringify(extensionsConfig, null, 2));
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Added ${(0, _intlayer_config_logger.colorize)(extensionId, _intlayer_config_colors.MAGENTA)} to ${(0, _intlayer_config_logger.colorizePath)(extensionsJsonPath)}`);
		} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(extensionsJsonPath)} already includes ${(0, _intlayer_config_logger.colorize)(extensionId, _intlayer_config_colors.MAGENTA)}`);
	} catch {
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not update ${(0, _intlayer_config_logger.colorizePath)(extensionsJsonPath)}. You may need to add ${(0, _intlayer_config_logger.colorize)(extensionId, _intlayer_config_colors.MAGENTA)} manually.`, { level: "warn" });
	}
	const settingsJsonPath = (0, node_path.join)(vscodeDir, "settings.json");
	if (!options?.noLsp) try {
		let settingsConfig = {};
		if (await require_init_utils_fileSystem.exists(rootDir, settingsJsonPath)) {
			const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, settingsJsonPath);
			settingsConfig = require_init_utils_jsonParser.parseJSONWithComments(content);
		} else await require_init_utils_fileSystem.ensureDirectory(rootDir, vscodeDir);
		let settingsUpdated = false;
		if (!settingsConfig["intlayer.languageServer.command"]) {
			settingsConfig["intlayer.languageServer.command"] = "npx";
			settingsUpdated = true;
		}
		if (!settingsConfig["intlayer.languageServer.args"]) {
			settingsConfig["intlayer.languageServer.args"] = ["@intlayer/lsp"];
			settingsUpdated = true;
		}
		if (settingsUpdated) {
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, settingsJsonPath, JSON.stringify(settingsConfig, null, 2));
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(settingsJsonPath)} with LSP configuration`);
		} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(settingsJsonPath)} already includes LSP configuration`);
	} catch {
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not update ${(0, _intlayer_config_logger.colorizePath)(settingsJsonPath)}. You may need to add the LSP settings manually.`, { level: "warn" });
	}
	if (!options?.noEslint && require_init_utils_packageManager.hasLintTooling(allDeps)) {
		const oxlintConfigPath = ".oxlintrc.json";
		if (await require_init_utils_fileSystem.exists(rootDir, oxlintConfigPath)) try {
			const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, oxlintConfigPath);
			const oxlintConfig = require_init_utils_jsonParser.parseJSONWithComments(content);
			oxlintConfig.jsPlugins ??= [];
			oxlintConfig.rules ??= {};
			oxlintConfig.ignorePatterns ??= [];
			let oxlintUpdated = false;
			for (const glob of GENERATED_OUTPUT_GLOBS) if (!oxlintConfig.ignorePatterns.includes(glob)) {
				oxlintConfig.ignorePatterns.push(glob);
				oxlintUpdated = true;
			}
			if (!oxlintConfig.jsPlugins.includes(LINT_PLUGIN_PACKAGE_NAME)) {
				oxlintConfig.jsPlugins.push(LINT_PLUGIN_PACKAGE_NAME);
				oxlintUpdated = true;
			}
			for (const [ruleName, severity] of Object.entries(getRecommendedRules())) if (!oxlintConfig.rules[ruleName]) {
				oxlintConfig.rules[ruleName] = severity;
				oxlintUpdated = true;
			}
			if (oxlintUpdated) {
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, oxlintConfigPath, JSON.stringify(oxlintConfig, null, 2));
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Added the Intlayer lint rules to ${(0, _intlayer_config_logger.colorizePath)(oxlintConfigPath)}`);
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(oxlintConfigPath)} already includes the Intlayer lint rules`);
		} catch {
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not update ${(0, _intlayer_config_logger.colorizePath)(oxlintConfigPath)}. You may need to add ${(0, _intlayer_config_logger.colorize)(LINT_PLUGIN_PACKAGE_NAME, _intlayer_config_colors.MAGENTA)} manually.`, { level: "warn" });
		}
		else (0, _intlayer_config_logger.logger)([
			(0, _intlayer_config_logger.colorize)("Intlayer lint rules →", _intlayer_config_colors.MAGENTA),
			(0, _intlayer_config_logger.colorize)(`Add ${LINT_PLUGIN_PACKAGE_NAME} to your ESLint flat config to catch hardcoded text and dynamic calls the compiler cannot optimize:`, _intlayer_config_colors.GREY_LIGHT),
			(0, _intlayer_config_logger.colorize)(`  import intlayer from '${LINT_PLUGIN_PACKAGE_NAME}';\n  export default [...intlayer.configs.recommended];`, _intlayer_config_colors.GREY_LIGHT),
			(0, _intlayer_config_logger.colorizePath)(DocumentationRouter.ESLint)
		]);
	}
	const tsConfigFiles = await require_init_utils_tsConfig.findTsConfigFiles(rootDir);
	let hasTsConfig = false;
	for (const fileName of tsConfigFiles) if (await require_init_utils_fileSystem.exists(rootDir, fileName)) {
		hasTsConfig = true;
		try {
			const fileContent = await require_init_utils_fileSystem.readFileFromRoot(rootDir, fileName);
			const config = require_init_utils_jsonParser.parseJSONWithComments(fileContent);
			const typeDefinition = "./.intlayer/**/*.ts";
			let updated = false;
			if (!config.include) {} else if (Array.isArray(config.include) && !config.include.some((pattern) => pattern.includes(".intlayer"))) {
				config.include.push(typeDefinition);
				updated = true;
			} else if (config.include.includes(typeDefinition)) (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(fileName)} already includes intlayer types`);
			if (updated) {
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, fileName, JSON.stringify(config, null, 2));
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(fileName)} to include intlayer types`);
			}
		} catch {
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not parse or update ${(0, _intlayer_config_logger.colorizePath)(fileName)}. You may need to add ${(0, _intlayer_config_logger.colorizePath)(".intlayer/types/**/*.ts")} manually.`, { level: "warn" });
		}
	}
	const format = hasTsConfig ? "intlayer.config.ts" : "intlayer.config.mjs";
	const detectedPattern = await require_init_utils_fileSystem.detectJsonLocalePattern(rootDir);
	await require_initConfig_index.initConfig(format, rootDir, {
		locales: detectedPattern?.locales,
		applicationURL: getDefaultApplicationURL(allDeps)
	});
	if (options?.contentStrategy === "centralized") await require_init_cms.setCompilerOutputInConfig(rootDir, "/locales/{{locale}}/{{key}}.content.json");
	const syncConfigToInject = compatSyncConfig ?? (wantsJsonNamespaces ? {
		format: "i18next",
		sourceTemplate: detectedPattern?.template ?? "./locales/${locale}/${key}.json"
	} : void 0);
	if (syncConfigToInject) {
		const nextIntlMessagesPattern = allDeps["next-intl"] || allDeps["@intlayer/next-intl"] || allDeps["use-intl"] || allDeps["@intlayer/use-intl"] ? await require_init_utils_fileSystem.detectNextIntlMessagesPattern(rootDir) : null;
		const sourceTemplate = linguiCatalog?.template ?? nextIntlMessagesPattern?.template ?? detectedPattern?.template;
		const resolvedSyncConfig = {
			...syncConfigToInject,
			...sourceTemplate ? { sourceTemplate } : {}
		};
		const syncPluginPackage = resolvedSyncConfig.plugin === "po" ? "@intlayer/sync-po-plugin" : "@intlayer/sync-json-plugin";
		const syncPluginName = resolvedSyncConfig.plugin === "po" ? "syncPO" : "syncJSON";
		if (resolvedSyncConfig.splitKeys && resolvedSyncConfig.sourceTemplate.includes("${key}")) resolvedSyncConfig.splitKeys = false;
		for (const configFile of [
			"intlayer.config.ts",
			"intlayer.config.mjs",
			"intlayer.config.js",
			"intlayer.config.cjs"
		]) if (await require_init_utils_fileSystem.exists(rootDir, configFile)) {
			const configContent = await require_init_utils_fileSystem.readFileFromRoot(rootDir, configFile);
			if (!configContent.includes(syncPluginPackage)) {
				const extension = configFile.split(".").pop();
				const updatedConfigContent = require_init_utils_configManipulation.updateIntlayerConfigWithSyncPlugin(configContent, extension, resolvedSyncConfig);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, configFile, updatedConfigContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(configFile)} with ${syncPluginName} compat plugin`);
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(configFile)} already includes ${syncPluginName} plugin`);
			break;
		}
	}
	if (options?.routingMode) await require_init_cms.setRoutingModeInConfig(rootDir, options.routingMode);
	let hasAliasConfiguration = false;
	for (const file of [
		"vite.config.ts",
		"vite.config.js",
		"vite.config.mjs"
	]) if (await require_init_utils_fileSystem.exists(rootDir, file)) {
		hasAliasConfiguration = true;
		const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, file);
		const extension = file.split(".").pop();
		if (compatVitePluginConfig) {
			const { replacesVitePlugin } = compatVitePluginConfig;
			if (content.includes(compatVitePluginConfig.pluginPackageSource)) (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(file)} already includes ${compatVitePluginConfig.pluginPackageSource}`);
			else if (replacesVitePlugin && content.includes(replacesVitePlugin.fromPackageSource)) {
				const updatedContent = require_init_utils_configManipulation.replaceViteConfigPluginImportSource(content, replacesVitePlugin.importName, replacesVitePlugin.fromPackageSource, compatVitePluginConfig.pluginPackageSource);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to import ${replacesVitePlugin.importName} from ${compatVitePluginConfig.pluginPackageSource}`);
			} else {
				const updatedContent = require_init_utils_configManipulation.updateViteConfigForCompatPlugin(content, extension, compatVitePluginConfig);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include ${compatVitePluginConfig.pluginFunctionName} compat plugin`);
			}
		} else if (require_init_utils_configManipulation.hasIntlayerVitePlugin(content)) (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(file)} already includes an Intlayer plugin`);
		else {
			const updatedContent = require_init_utils_configManipulation.updateViteConfig(content, extension);
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer plugin`);
		}
		break;
	}
	const nextConfigs = [
		"next.config.js",
		"next.config.mjs",
		"next.config.ts"
	];
	let isNextJsProject = false;
	for (const file of nextConfigs) if (await require_init_utils_fileSystem.exists(rootDir, file)) {
		isNextJsProject = true;
		hasAliasConfiguration = true;
		const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, file);
		const extension = file.split(".").pop();
		if (allDeps["next-i18next"]) {
			if (!content.includes("@intlayer/next-i18next")) {
				const updatedContent = require_init_utils_configManipulation.updateNextConfigForNextI18next(content, extension);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer next-i18next compat plugin`);
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(file)} already includes @intlayer/next-i18next`);
		} else if (allDeps["next-intl"]) {
			if (!content.includes("@intlayer/next-intl/plugin")) {
				const updatedContent = require_init_utils_configManipulation.updateNextConfigForNextIntl(content, extension);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer next-intl compat plugin`);
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(file)} already includes @intlayer/next-intl/plugin`);
		} else if (allDeps["next-translate"]) {
			if (!content.includes("@intlayer/next-translate")) {
				const updatedContent = require_init_utils_configManipulation.updateNextConfigForNextTranslate(content, extension);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer next-translate compat plugin`);
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(file)} already includes @intlayer/next-translate`);
		} else if (!content.includes("next-intlayer")) {
			const updatedContent = require_init_utils_configManipulation.updateNextConfig(content, extension);
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer plugin`);
		}
		break;
	}
	if (isNextJsProject) await require_init_utils_nextCompilerBabel.setupNextCompilerBabelConfig({
		rootDir,
		packageManager,
		allDeps,
		skipInstall: Boolean(options?.noInstallPackages)
	});
	for (const file of [
		"astro.config.mjs",
		"astro.config.js",
		"astro.config.ts",
		"astro.config.cjs"
	]) if (await require_init_utils_fileSystem.exists(rootDir, file)) {
		hasAliasConfiguration = true;
		if (file.startsWith("astro.config.")) {
			const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, file);
			if (!content.includes("astro-intlayer")) {
				const extension = file.split(".").pop();
				const updatedContent = require_init_utils_configManipulation.updateAstroConfig(content, extension);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer integration`);
			}
		}
		break;
	}
	for (const file of ["nuxt.config.js", "nuxt.config.ts"]) if (await require_init_utils_fileSystem.exists(rootDir, file)) {
		hasAliasConfiguration = true;
		const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, file);
		if (allDeps["@nuxtjs/i18n"]) {
			if (!content.includes("@intlayer/nuxtjs-i18n")) {
				const updatedContent = require_init_utils_configManipulation.updateNuxtConfigForNuxtjsI18n(content);
				await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
				(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include @intlayer/nuxtjs-i18n module`);
			} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(file)} already includes @intlayer/nuxtjs-i18n`);
		} else if (!content.includes("nuxt-intlayer")) {
			const updatedContent = require_init_utils_configManipulation.updateNuxtConfig(content);
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, file, updatedContent);
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(file)} to include Intlayer module`);
		}
		break;
	}
	if (Boolean(allDeps["react-native"] || allDeps.expo)) {
		hasAliasConfiguration = true;
		const metroConfigs = [
			"metro.config.js",
			"metro.config.cjs",
			"metro.config.mjs",
			"metro.config.ts"
		];
		let metroConfigFile;
		for (const file of metroConfigs) if (await require_init_utils_fileSystem.exists(rootDir, file)) {
			metroConfigFile = file;
			break;
		}
		if (metroConfigFile) {
			const content = await require_init_utils_fileSystem.readFileFromRoot(rootDir, metroConfigFile);
			if (content.includes("react-native-intlayer")) (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorizePath)(metroConfigFile)} already includes the Intlayer Metro plugin`);
			else {
				const extension = metroConfigFile.split(".").pop();
				const updatedContent = require_init_utils_configManipulation.updateMetroConfig(content, extension);
				if (updatedContent !== content) {
					await require_init_utils_fileSystem.writeFileToRoot(rootDir, metroConfigFile, updatedContent);
					(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(metroConfigFile)} to include the Intlayer Metro plugin`);
				} else (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Could not automatically update ${(0, _intlayer_config_logger.colorizePath)(metroConfigFile)}. Wrap your exported config with ${(0, _intlayer_config_logger.colorize)("configMetroIntlayer", _intlayer_config_colors.MAGENTA)}: ${(0, _intlayer_config_logger.colorizePath)(DocumentationRouter.ReactNativeAndExpo)}`, { level: "warn" });
			}
		} else {
			const newMetroConfigFile = "metro.config.js";
			await require_init_utils_fileSystem.writeFileToRoot(rootDir, newMetroConfigFile, require_init_utils_configManipulation.getMetroConfigTemplate(Boolean(allDeps.expo)));
			(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Created ${(0, _intlayer_config_logger.colorizePath)(newMetroConfigFile)} with the Intlayer Metro plugin`);
		}
	}
	const backendIntlayerPackages = [
		"express-intlayer",
		"fastify-intlayer",
		"adonis-intlayer",
		"hono-intlayer"
	];
	const devScript = packageJson.scripts?.dev;
	let newDevScript;
	if (devScript && (isNextJsProject && allDeps.next && require_init_utils_version.isVersionAtLeast(allDeps.next, 16) || backendIntlayerPackages.some((pkg) => allDeps[pkg])) && !devScript.includes("intlayer watch")) newDevScript = `intlayer watch --with '${devScript}'`;
	if (newDevScript) {
		packageJson.scripts.dev = newDevScript;
		await require_init_utils_fileSystem.writeFileToRoot(rootDir, packageJsonPath, JSON.stringify(packageJson, null, 2));
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)("package.json")} dev script to run intlayer watch`);
	}
	for (const file of [
		"webpack.config.js",
		"webpack.config.ts",
		"webpack.config.mjs",
		"webpack.config.cjs"
	]) if (await require_init_utils_fileSystem.exists(rootDir, file)) {
		hasAliasConfiguration = true;
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Found ${(0, _intlayer_config_logger.colorizePath)(file)}. Make sure to configure aliases manually or use the Intlayer Webpack plugin.`);
		break;
	}
	if ([
		"express",
		"fastify",
		"@adonisjs/core",
		"hono",
		...backendIntlayerPackages
	].some((pkg) => allDeps[pkg])) hasAliasConfiguration = true;
	if (!hasAliasConfiguration) {
		const configuration = (0, _intlayer_config_node.getConfiguration)({ baseDir: rootDir });
		const aliases = (0, _intlayer_config_utils.getAlias)({ configuration });
		if (hasTsConfig && tsConfigFiles.length > 0) {
			const tsConfigPath = tsConfigFiles.find((file) => file === "tsconfig.json") || tsConfigFiles[0];
			if (tsConfigPath) {
				const tsConfigContent = await require_init_utils_fileSystem.readFileFromRoot(rootDir, tsConfigPath);
				const config = require_init_utils_jsonParser.parseJSONWithComments(tsConfigContent);
				config.compilerOptions ??= {};
				config.compilerOptions.paths ??= {};
				let updated = false;
				Object.entries(aliases).forEach(([alias, path]) => {
					if (!config.compilerOptions.paths[alias]) {
						config.compilerOptions.paths[alias] = [path];
						updated = true;
					}
				});
				if (updated) {
					await require_init_utils_fileSystem.writeFileToRoot(rootDir, tsConfigPath, JSON.stringify(config, null, 2));
					(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(tsConfigPath)} to include Intlayer aliases`);
				}
			}
		} else {
			const jsConfigPath = "jsconfig.json";
			if (await require_init_utils_fileSystem.exists(rootDir, jsConfigPath)) {
				const jsConfigContent = await require_init_utils_fileSystem.readFileFromRoot(rootDir, jsConfigPath);
				const config = require_init_utils_jsonParser.parseJSONWithComments(jsConfigContent);
				config.compilerOptions ??= {};
				config.compilerOptions.paths ??= {};
				let updated = false;
				Object.entries(aliases).forEach(([alias, path]) => {
					if (!config.compilerOptions.paths[alias]) {
						config.compilerOptions.paths[alias] = [path];
						updated = true;
					}
				});
				if (updated) {
					await require_init_utils_fileSystem.writeFileToRoot(rootDir, jsConfigPath, JSON.stringify(config, null, 2));
					(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(jsConfigPath)} to include Intlayer aliases`);
				}
			} else {
				packageJson.imports ??= {};
				let updated = false;
				Object.entries(aliases).forEach(([alias, path]) => {
					const importAlias = alias.replace("@", "#");
					const importPath = path.startsWith(".") ? path : `./${path}`;
					if (!packageJson.imports[importAlias]) {
						packageJson.imports[importAlias] = importPath;
						updated = true;
					}
				});
				if (updated) {
					await require_init_utils_fileSystem.writeFileToRoot(rootDir, packageJsonPath, JSON.stringify(packageJson, null, 2));
					(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Updated ${(0, _intlayer_config_logger.colorizePath)(packageJsonPath)} to include Intlayer imports`);
				}
			}
		}
	}
	const hasCompatLibrary = Boolean(compatSyncConfig || compatVitePluginConfig);
	if (!options?.noFrameworkSetup && hasCompatLibrary) (0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} Compat i18n library detected — keeping the app structure untouched (no route restructure, no provider injection)`);
	if (!options?.noFrameworkSetup && !hasCompatLibrary) try {
		const routingMode = options?.routingMode ?? (0, _intlayer_config_node.getConfiguration)({ baseDir: rootDir }).routing.mode;
		await require_init_frameworkSetup_index.setupFramework({
			rootDir,
			allDeps,
			packageManager,
			useTypeScript: hasTsConfig,
			routingMode
		});
	} catch {
		(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.x} Framework-specific scaffolding failed. Your existing files were left untouched; follow the documentation to finish the setup.`, { level: "warn" });
	}
	(0, _intlayer_config_logger.logger)(`${_intlayer_config_logger.v} ${(0, _intlayer_config_logger.colorize)("Intlayer init setup complete.", _intlayer_config_colors.GREEN)}`);
	(0, _intlayer_config_logger.logger)([
		(0, _intlayer_config_logger.colorize)("Next →", _intlayer_config_colors.MAGENTA),
		(0, _intlayer_config_logger.colorize)(`Follow the instructions in the documentation to complete the setup:`, _intlayer_config_colors.GREY_LIGHT),
		(0, _intlayer_config_logger.colorizePath)(guideUrl)
	]);
	(0, _intlayer_config_logger.logger)([
		(0, _intlayer_config_logger.colorize)("Intlayer LSP →", _intlayer_config_colors.MAGENTA),
		(0, _intlayer_config_logger.colorize)("The Intlayer Language Server is available via the `@intlayer/lsp` package. It lets IDEs and AI agents jump from `useIntlayer`/`getIntlayer` keys to their `.content` files.", _intlayer_config_colors.GREY_LIGHT),
		(0, _intlayer_config_logger.colorize)("Ask the user which platform/editor they want to enable it on (VS Code, Cursor, Windsurf, Zed, Neovim, Claude Code, …), then follow the matching setup instructions:", _intlayer_config_colors.GREY_LIGHT),
		(0, _intlayer_config_logger.colorizePath)(DocumentationRouter.LSP)
	]);
};

//#endregion
exports.getDefaultApplicationURL = getDefaultApplicationURL;
exports.initIntlayer = initIntlayer;
//# sourceMappingURL=index.cjs.map