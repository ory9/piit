import { getMetroConfigTemplate, hasIntlayerVitePlugin, replaceViteConfigPluginImportSource, updateAstroConfig, updateIntlayerConfigWithSyncPlugin, updateMetroConfig, updateNextConfig, updateNextConfigForNextI18next, updateNextConfigForNextIntl, updateNextConfigForNextTranslate, updateNuxtConfig, updateNuxtConfigForNuxtjsI18n, updateViteConfig, updateViteConfigForCompatPlugin } from "./utils/configManipulation.mjs";
import { detectJsonLocalePattern, detectLinguiCatalogPattern, detectNextIntlMessagesPattern, ensureDirectory, exists, readFileFromRoot, writeFileToRoot } from "./utils/fileSystem.mjs";
import { setCompilerOutputInConfig, setRoutingModeInConfig } from "./cms.mjs";
import { initConfig } from "../initConfig/index.mjs";
import { isVersionAtLeast } from "./utils/version.mjs";
import { setupFramework } from "./frameworkSetup/index.mjs";
import { detectMissingIntlayerPackages, detectOutdatedIntlayerPackages, detectPackageManager, hasLintTooling, installPackages, upgradePackages } from "./utils/packageManager.mjs";
import { getGithubWorkflows, resolveGithubWorkflowsContext } from "./utils/githubActions.mjs";
import { parseJSONWithComments } from "./utils/jsonParser.mjs";
import { setupNextCompilerBabelConfig } from "./utils/nextCompilerBabel.mjs";
import { findTsConfigFiles } from "./utils/tsConfig.mjs";
import { join } from "node:path";
import { colorize, colorizePath, logger, v, x } from "@intlayer/config/logger";
import { getConfiguration } from "@intlayer/config/node";
import { getAlias } from "@intlayer/config/utils";
import * as ANSIColors from "@intlayer/config/colors";

//#region src/init/index.ts
/**
* Documentation URL Constants
*/
const DocumentationRouter = {
	NextJS: "https://intlayer.org/doc/environment/nextjs.md",
	NextJS_15: "https://intlayer.org/doc/environment/nextjs/15.md",
	NextJS_14: "https://intlayer.org/doc/environment/nextjs/14.md",
	CRA: "https://intlayer.org/doc/environment/create-react-app.md",
	Astro: "https://intlayer.org/doc/environment/astro.md",
	ViteAndReact: "https://intlayer.org/doc/environment/vite-and-react.md",
	ViteAndReact_ReactRouterV7: "https://intlayer.org/doc/environment/vite-and-react/react-router-v7.md",
	ViteAndReact_ReactRouterV7_FSRoutes: "https://intlayer.org/doc/environment/vite-and-react/react-router-v7-fs-routes.md",
	ViteAndVue: "https://intlayer.org/doc/environment/vite-and-vue.md",
	ViteAndSolid: "https://intlayer.org/doc/environment/vite-and-solid.md",
	ViteAndSvelte: "https://intlayer.org/doc/environment/vite-and-svelte.md",
	ViteAndPreact: "https://intlayer.org/doc/environment/vite-and-preact.md",
	TanStackRouter: "https://intlayer.org/doc/environment/tanstack-start.md",
	NuxtAndVue: "https://intlayer.org/doc/environment/nuxt-and-vue.md",
	Angular: "https://intlayer.org/doc/environment/angular.md",
	SvelteKit: "https://intlayer.org/doc/environment/sveltekit.md",
	ReactNativeAndExpo: "https://intlayer.org/doc/environment/react-native-and-expo.md",
	Lynx: "https://intlayer.org/doc/environment/lynx-and-react.md",
	Express: "https://intlayer.org/doc/environment/express.md",
	NestJS: "https://intlayer.org/doc/environment/nest.md",
	Fastify: "https://intlayer.org/doc/environment/fastify.md",
	Default: "https://intlayer.org/doc/get-started",
	LSP: "https://intlayer.org/doc/lsp.md",
	ESLint: "https://intlayer.org/doc/eslint.md",
	NextIntl: "https://intlayer.org/blog/intlayer-with-next-intl.md",
	ReactI18Next: "https://intlayer.org/blog/intlayer-with-react-i18next.md",
	ReactIntl: "https://intlayer.org/blog/intlayer-with-react-intl.md",
	NextI18Next: "https://intlayer.org/blog/intlayer-with-next-i18next.md",
	VueI18n: "https://intlayer.org/blog/intlayer-with-vue-i18n.md"
};
/**
* The Intlayer lint setup, as `intlayer init` writes it into `.oxlintrc.json`.
*
* Declared locally on purpose: `@intlayer/engine` is installed by every Intlayer
* user, so it must not depend on a lint plugin to write a handful of strings
* into a JSON file. oxlint resolves a plugin's *rules* but not its shipped
* *configs*, so the preset cannot be referenced by name there either.
*
* Mirrors the `recommended` preset of `eslint-plugin-intlayer`; keep in sync
* when a rule is enabled by default there.
*/
const LINT_PLUGIN_PACKAGE_NAME = "eslint-plugin-intlayer";
/** Intlayer's generated output — never source, never worth linting. */
const GENERATED_OUTPUT_GLOBS = ["**/.intlayer/**"];
/** Rules of the `recommended` preset, prefixed by the plugin namespace. */
const getRecommendedRules = () => ({
	"intlayer/no-raw-text": "warn",
	"intlayer/static-dictionary-key": "error",
	"intlayer/no-dynamic-field-access": "error"
});
/**
* Helper: Detects the environment and returns the doc URL
*/
const getDocumentationUrl = (packageJson) => {
	const deps = {
		...packageJson.dependencies,
		...packageJson.devDependencies
	};
	/**
	* Helper to check if a version string matches a specific major version
	* Matches: "15", "^15.0.0", "~15.2", "15.0.0-beta"
	*/
	const isVersion = (versionString, major) => {
		if (!versionString || typeof versionString !== "string") return false;
		return new RegExp(`^[\\^~]?${major}(?:\\.|$)`).test(versionString);
	};
	if (deps["@lynx-js/react"] || deps["@lynx-js/core"]) return DocumentationRouter.Lynx;
	if (deps["react-native"] || deps.expo) return DocumentationRouter.ReactNativeAndExpo;
	if (deps.next) {
		const version = deps.next;
		if (isVersion(version, 14)) return DocumentationRouter.NextJS_14;
		if (isVersion(version, 15)) return DocumentationRouter.NextJS_15;
		return DocumentationRouter.NextJS;
	}
	if (deps.nuxt) return DocumentationRouter.NuxtAndVue;
	if (deps.astro) return DocumentationRouter.Astro;
	if (deps["@sveltejs/kit"]) return DocumentationRouter.SvelteKit;
	if (deps["@tanstack/react-router"]) return DocumentationRouter.TanStackRouter;
	const reactRouterVersion = deps["react-router"];
	if (reactRouterVersion && typeof reactRouterVersion === "string") {
		if (deps["@react-router/fs-routes"]) return DocumentationRouter.ViteAndReact_ReactRouterV7_FSRoutes;
		if (isVersion(reactRouterVersion, 7)) return DocumentationRouter.ViteAndReact_ReactRouterV7;
	}
	if (deps.vite) {
		if (deps.vue) return DocumentationRouter.ViteAndVue;
		if (deps["solid-js"]) return DocumentationRouter.ViteAndSolid;
		if (deps.svelte) return DocumentationRouter.ViteAndSvelte;
		if (deps.preact) return DocumentationRouter.ViteAndPreact;
		return DocumentationRouter.ViteAndReact;
	}
	if (deps["react-scripts"]) return DocumentationRouter.CRA;
	if (deps["@angular/core"]) return DocumentationRouter.Angular;
	if (deps["@nestjs/core"]) return DocumentationRouter.NestJS;
	if (deps.express) return DocumentationRouter.Express;
	if (deps.fastify) return DocumentationRouter.Fastify;
	if (deps["next-intl"]) return DocumentationRouter.NextIntl;
	if (deps["react-i18next"] || deps.i18next) return DocumentationRouter.ReactI18Next;
	if (deps["react-intl"]) return DocumentationRouter.ReactIntl;
	if (deps["next-i18next"]) return DocumentationRouter.NextI18Next;
	if (deps["vue-i18n"]) return DocumentationRouter.VueI18n;
	return DocumentationRouter.Default;
};
/**
* Resolves the default dev-server URL of the detected framework, used as the
* `editor.applicationURL` of the generated configuration file. Frameworks that
* embed Vite (Astro, SvelteKit, …) are checked before the bare Vite fallback so
* their own dev port wins.
*/
const getDefaultApplicationURL = (dependencies) => {
	if (dependencies.next) return "http://localhost:3000";
	if (dependencies.astro) return "http://localhost:4321";
	if (dependencies["@angular/core"]) return "http://localhost:4200";
	if (dependencies.nuxt) return "http://localhost:3000";
	if (dependencies["@sveltejs/kit"]) return "http://localhost:5173";
	if (dependencies["react-scripts"]) return "http://localhost:3000";
	if (dependencies.vite) return "http://localhost:5173";
	return "http://localhost:3000";
};
/**
* MAIN LOGIC
*/
const initIntlayer = async (rootDir, options) => {
	logger(colorize("Checking Intlayer configuration...", ANSIColors.CYAN));
	const packageJsonPath = "package.json";
	if (!await exists(rootDir, packageJsonPath)) {
		logger(`${x} No ${colorizePath("package.json")} found. Please run this script from the project root.`, { level: "error" });
		process.exit(1);
	}
	const packageJsonContent = await readFileFromRoot(rootDir, packageJsonPath);
	let packageJson;
	try {
		packageJson = JSON.parse(packageJsonContent);
	} catch {
		logger(`${x} Could not parse ${colorizePath("package.json")}.`, { level: "error" });
		process.exit(1);
	}
	const guideUrl = getDocumentationUrl(packageJson);
	const allDeps = {
		...packageJson.dependencies ?? {},
		...packageJson.devDependencies ?? {}
	};
	const packageManager = detectPackageManager(rootDir);
	const linguiPresent = Boolean(allDeps["@lingui/core"] || allDeps["@lingui/react"] || allDeps["@intlayer/lingui"]);
	const linguiCatalog = linguiPresent ? await detectLinguiCatalogPattern(rootDir) : null;
	const linguiCatalogFormat = linguiCatalog?.format ?? (linguiPresent ? "po" : null);
	const { packagesToInstall, devPackagesToInstall, compatSyncConfig, compatVitePluginConfig } = detectMissingIntlayerPackages(allDeps, { linguiCatalogFormat });
	const wantsJsonNamespaces = options?.contentStrategy === "json-namespaces" && !compatSyncConfig;
	if (wantsJsonNamespaces && !allDeps["@intlayer/sync-json-plugin"] && !devPackagesToInstall.includes("@intlayer/sync-json-plugin")) devPackagesToInstall.push("@intlayer/sync-json-plugin");
	if (!options?.noInstallPackages) {
		const withVersion = (packages) => options?.upgradeToVersion ? packages.map((pkg) => `${pkg}@${options.upgradeToVersion}`) : packages;
		if (packagesToInstall.length > 0) {
			logger(colorize("Installing missing Intlayer dependencies...", ANSIColors.CYAN));
			try {
				installPackages(rootDir, withVersion(packagesToInstall), packageManager);
				logger(`${v} Installed: ${packagesToInstall.map((pkg) => colorize(pkg, ANSIColors.MAGENTA)).join(", ")}`);
			} catch {
				logger(`${x} Failed to install packages. Please install manually: ${packagesToInstall.join(" ")}`, { level: "warn" });
			}
		}
		if (devPackagesToInstall.length > 0) {
			logger(colorize("Installing missing Intlayer dev dependencies...", ANSIColors.CYAN));
			try {
				installPackages(rootDir, withVersion(devPackagesToInstall), packageManager, true);
				logger(`${v} Installed: ${devPackagesToInstall.map((pkg) => colorize(pkg, ANSIColors.MAGENTA)).join(", ")}`);
			} catch {
				logger(`${x} Failed to install dev packages. Please install manually: ${devPackagesToInstall.join(" ")}`, { level: "warn" });
			}
		}
		if (options?.upgradeToVersion) {
			const outdatedDependencies = detectOutdatedIntlayerPackages(rootDir, packageJson.dependencies ?? {}, options.upgradeToVersion);
			const outdatedDevDependencies = detectOutdatedIntlayerPackages(rootDir, packageJson.devDependencies ?? {}, options.upgradeToVersion);
			const allOutdated = [...outdatedDependencies, ...outdatedDevDependencies];
			if (allOutdated.length > 0) {
				logger(colorize(`Upgrading outdated Intlayer dependencies to ${options.upgradeToVersion}...`, ANSIColors.CYAN));
				try {
					upgradePackages(rootDir, outdatedDependencies, packageManager, options.upgradeToVersion);
					upgradePackages(rootDir, outdatedDevDependencies, packageManager, options.upgradeToVersion, true);
					logger(`${v} Upgraded: ${allOutdated.map((pkg) => colorize(pkg, ANSIColors.MAGENTA)).join(", ")}`);
				} catch {
					logger(`${x} Failed to upgrade packages. Please upgrade manually: ${allOutdated.join(" ")}`, { level: "warn" });
				}
			} else logger(`${v} Intlayer dependencies are up to date`);
		}
	}
	const gitignorePath = ".gitignore";
	if (!options?.noGitignore && await exists(rootDir, gitignorePath)) {
		const gitignoreContent = await readFileFromRoot(rootDir, gitignorePath);
		if (!gitignoreContent.includes("intlayer")) {
			const newContent = `${gitignoreContent}\n# Intlayer\n.intlayer\n`;
			await writeFileToRoot(rootDir, gitignorePath, newContent);
			logger(`${v} Added ${colorizePath(".intlayer")} to ${colorizePath(gitignorePath)}`);
		} else logger(`${v} ${colorizePath(gitignorePath)} already includes .intlayer`);
	}
	if (!options?.noGithubActions) {
		const workflowsContext = await resolveGithubWorkflowsContext(rootDir, packageManager);
		const workflows = getGithubWorkflows(workflowsContext.packageManager, workflowsContext.options);
		for (const workflow of workflows) {
			if (await exists(workflowsContext.workflowsRootDir, workflow.filePath)) {
				logger(`${v} ${colorizePath(workflow.filePath)} already exists`);
				continue;
			}
			try {
				await ensureDirectory(workflowsContext.workflowsRootDir, join(".github", "workflows"));
				await writeFileToRoot(workflowsContext.workflowsRootDir, workflow.filePath, workflow.content);
				logger(`${v} Added GitHub Actions workflow ${colorizePath(workflow.filePath)}`);
			} catch {
				logger(`${x} Could not create ${colorizePath(workflow.filePath)}. You may need to add it manually.`, { level: "warn" });
			}
		}
	}
	const vscodeDir = ".vscode";
	const extensionsJsonPath = join(vscodeDir, "extensions.json");
	const extensionId = "intlayer.intlayer-vs-code-extension";
	if (!options?.noVscodeExtension) try {
		let extensionsConfig = { recommendations: [] };
		if (await exists(rootDir, extensionsJsonPath)) {
			const content = await readFileFromRoot(rootDir, extensionsJsonPath);
			extensionsConfig = parseJSONWithComments(content);
		} else await ensureDirectory(rootDir, vscodeDir);
		if (!extensionsConfig.recommendations) extensionsConfig.recommendations = [];
		if (!extensionsConfig.recommendations.includes(extensionId)) {
			extensionsConfig.recommendations.push(extensionId);
			await writeFileToRoot(rootDir, extensionsJsonPath, JSON.stringify(extensionsConfig, null, 2));
			logger(`${v} Added ${colorize(extensionId, ANSIColors.MAGENTA)} to ${colorizePath(extensionsJsonPath)}`);
		} else logger(`${v} ${colorizePath(extensionsJsonPath)} already includes ${colorize(extensionId, ANSIColors.MAGENTA)}`);
	} catch {
		logger(`${x} Could not update ${colorizePath(extensionsJsonPath)}. You may need to add ${colorize(extensionId, ANSIColors.MAGENTA)} manually.`, { level: "warn" });
	}
	const settingsJsonPath = join(vscodeDir, "settings.json");
	if (!options?.noLsp) try {
		let settingsConfig = {};
		if (await exists(rootDir, settingsJsonPath)) {
			const content = await readFileFromRoot(rootDir, settingsJsonPath);
			settingsConfig = parseJSONWithComments(content);
		} else await ensureDirectory(rootDir, vscodeDir);
		let settingsUpdated = false;
		if (!settingsConfig["intlayer.languageServer.command"]) {
			settingsConfig["intlayer.languageServer.command"] = "npx";
			settingsUpdated = true;
		}
		if (!settingsConfig["intlayer.languageServer.args"]) {
			settingsConfig["intlayer.languageServer.args"] = ["@intlayer/lsp"];
			settingsUpdated = true;
		}
		if (settingsUpdated) {
			await writeFileToRoot(rootDir, settingsJsonPath, JSON.stringify(settingsConfig, null, 2));
			logger(`${v} Updated ${colorizePath(settingsJsonPath)} with LSP configuration`);
		} else logger(`${v} ${colorizePath(settingsJsonPath)} already includes LSP configuration`);
	} catch {
		logger(`${x} Could not update ${colorizePath(settingsJsonPath)}. You may need to add the LSP settings manually.`, { level: "warn" });
	}
	if (!options?.noEslint && hasLintTooling(allDeps)) {
		const oxlintConfigPath = ".oxlintrc.json";
		if (await exists(rootDir, oxlintConfigPath)) try {
			const content = await readFileFromRoot(rootDir, oxlintConfigPath);
			const oxlintConfig = parseJSONWithComments(content);
			oxlintConfig.jsPlugins ??= [];
			oxlintConfig.rules ??= {};
			oxlintConfig.ignorePatterns ??= [];
			let oxlintUpdated = false;
			for (const glob of GENERATED_OUTPUT_GLOBS) if (!oxlintConfig.ignorePatterns.includes(glob)) {
				oxlintConfig.ignorePatterns.push(glob);
				oxlintUpdated = true;
			}
			if (!oxlintConfig.jsPlugins.includes(LINT_PLUGIN_PACKAGE_NAME)) {
				oxlintConfig.jsPlugins.push(LINT_PLUGIN_PACKAGE_NAME);
				oxlintUpdated = true;
			}
			for (const [ruleName, severity] of Object.entries(getRecommendedRules())) if (!oxlintConfig.rules[ruleName]) {
				oxlintConfig.rules[ruleName] = severity;
				oxlintUpdated = true;
			}
			if (oxlintUpdated) {
				await writeFileToRoot(rootDir, oxlintConfigPath, JSON.stringify(oxlintConfig, null, 2));
				logger(`${v} Added the Intlayer lint rules to ${colorizePath(oxlintConfigPath)}`);
			} else logger(`${v} ${colorizePath(oxlintConfigPath)} already includes the Intlayer lint rules`);
		} catch {
			logger(`${x} Could not update ${colorizePath(oxlintConfigPath)}. You may need to add ${colorize(LINT_PLUGIN_PACKAGE_NAME, ANSIColors.MAGENTA)} manually.`, { level: "warn" });
		}
		else logger([
			colorize("Intlayer lint rules →", ANSIColors.MAGENTA),
			colorize(`Add ${LINT_PLUGIN_PACKAGE_NAME} to your ESLint flat config to catch hardcoded text and dynamic calls the compiler cannot optimize:`, ANSIColors.GREY_LIGHT),
			colorize(`  import intlayer from '${LINT_PLUGIN_PACKAGE_NAME}';\n  export default [...intlayer.configs.recommended];`, ANSIColors.GREY_LIGHT),
			colorizePath(DocumentationRouter.ESLint)
		]);
	}
	const tsConfigFiles = await findTsConfigFiles(rootDir);
	let hasTsConfig = false;
	for (const fileName of tsConfigFiles) if (await exists(rootDir, fileName)) {
		hasTsConfig = true;
		try {
			const fileContent = await readFileFromRoot(rootDir, fileName);
			const config = parseJSONWithComments(fileContent);
			const typeDefinition = "./.intlayer/**/*.ts";
			let updated = false;
			if (!config.include) {} else if (Array.isArray(config.include) && !config.include.some((pattern) => pattern.includes(".intlayer"))) {
				config.include.push(typeDefinition);
				updated = true;
			} else if (config.include.includes(typeDefinition)) logger(`${v} ${colorizePath(fileName)} already includes intlayer types`);
			if (updated) {
				await writeFileToRoot(rootDir, fileName, JSON.stringify(config, null, 2));
				logger(`${v} Updated ${colorizePath(fileName)} to include intlayer types`);
			}
		} catch {
			logger(`${x} Could not parse or update ${colorizePath(fileName)}. You may need to add ${colorizePath(".intlayer/types/**/*.ts")} manually.`, { level: "warn" });
		}
	}
	const format = hasTsConfig ? "intlayer.config.ts" : "intlayer.config.mjs";
	const detectedPattern = await detectJsonLocalePattern(rootDir);
	await initConfig(format, rootDir, {
		locales: detectedPattern?.locales,
		applicationURL: getDefaultApplicationURL(allDeps)
	});
	if (options?.contentStrategy === "centralized") await setCompilerOutputInConfig(rootDir, "/locales/{{locale}}/{{key}}.content.json");
	const syncConfigToInject = compatSyncConfig ?? (wantsJsonNamespaces ? {
		format: "i18next",
		sourceTemplate: detectedPattern?.template ?? "./locales/${locale}/${key}.json"
	} : void 0);
	if (syncConfigToInject) {
		const nextIntlMessagesPattern = allDeps["next-intl"] || allDeps["@intlayer/next-intl"] || allDeps["use-intl"] || allDeps["@intlayer/use-intl"] ? await detectNextIntlMessagesPattern(rootDir) : null;
		const sourceTemplate = linguiCatalog?.template ?? nextIntlMessagesPattern?.template ?? detectedPattern?.template;
		const resolvedSyncConfig = {
			...syncConfigToInject,
			...sourceTemplate ? { sourceTemplate } : {}
		};
		const syncPluginPackage = resolvedSyncConfig.plugin === "po" ? "@intlayer/sync-po-plugin" : "@intlayer/sync-json-plugin";
		const syncPluginName = resolvedSyncConfig.plugin === "po" ? "syncPO" : "syncJSON";
		if (resolvedSyncConfig.splitKeys && resolvedSyncConfig.sourceTemplate.includes("${key}")) resolvedSyncConfig.splitKeys = false;
		for (const configFile of [
			"intlayer.config.ts",
			"intlayer.config.mjs",
			"intlayer.config.js",
			"intlayer.config.cjs"
		]) if (await exists(rootDir, configFile)) {
			const configContent = await readFileFromRoot(rootDir, configFile);
			if (!configContent.includes(syncPluginPackage)) {
				const extension = configFile.split(".").pop();
				const updatedConfigContent = updateIntlayerConfigWithSyncPlugin(configContent, extension, resolvedSyncConfig);
				await writeFileToRoot(rootDir, configFile, updatedConfigContent);
				logger(`${v} Updated ${colorizePath(configFile)} with ${syncPluginName} compat plugin`);
			} else logger(`${v} ${colorizePath(configFile)} already includes ${syncPluginName} plugin`);
			break;
		}
	}
	if (options?.routingMode) await setRoutingModeInConfig(rootDir, options.routingMode);
	let hasAliasConfiguration = false;
	for (const file of [
		"vite.config.ts",
		"vite.config.js",
		"vite.config.mjs"
	]) if (await exists(rootDir, file)) {
		hasAliasConfiguration = true;
		const content = await readFileFromRoot(rootDir, file);
		const extension = file.split(".").pop();
		if (compatVitePluginConfig) {
			const { replacesVitePlugin } = compatVitePluginConfig;
			if (content.includes(compatVitePluginConfig.pluginPackageSource)) logger(`${v} ${colorizePath(file)} already includes ${compatVitePluginConfig.pluginPackageSource}`);
			else if (replacesVitePlugin && content.includes(replacesVitePlugin.fromPackageSource)) {
				const updatedContent = replaceViteConfigPluginImportSource(content, replacesVitePlugin.importName, replacesVitePlugin.fromPackageSource, compatVitePluginConfig.pluginPackageSource);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to import ${replacesVitePlugin.importName} from ${compatVitePluginConfig.pluginPackageSource}`);
			} else {
				const updatedContent = updateViteConfigForCompatPlugin(content, extension, compatVitePluginConfig);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to include ${compatVitePluginConfig.pluginFunctionName} compat plugin`);
			}
		} else if (hasIntlayerVitePlugin(content)) logger(`${v} ${colorizePath(file)} already includes an Intlayer plugin`);
		else {
			const updatedContent = updateViteConfig(content, extension);
			await writeFileToRoot(rootDir, file, updatedContent);
			logger(`${v} Updated ${colorizePath(file)} to include Intlayer plugin`);
		}
		break;
	}
	const nextConfigs = [
		"next.config.js",
		"next.config.mjs",
		"next.config.ts"
	];
	let isNextJsProject = false;
	for (const file of nextConfigs) if (await exists(rootDir, file)) {
		isNextJsProject = true;
		hasAliasConfiguration = true;
		const content = await readFileFromRoot(rootDir, file);
		const extension = file.split(".").pop();
		if (allDeps["next-i18next"]) {
			if (!content.includes("@intlayer/next-i18next")) {
				const updatedContent = updateNextConfigForNextI18next(content, extension);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to include Intlayer next-i18next compat plugin`);
			} else logger(`${v} ${colorizePath(file)} already includes @intlayer/next-i18next`);
		} else if (allDeps["next-intl"]) {
			if (!content.includes("@intlayer/next-intl/plugin")) {
				const updatedContent = updateNextConfigForNextIntl(content, extension);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to include Intlayer next-intl compat plugin`);
			} else logger(`${v} ${colorizePath(file)} already includes @intlayer/next-intl/plugin`);
		} else if (allDeps["next-translate"]) {
			if (!content.includes("@intlayer/next-translate")) {
				const updatedContent = updateNextConfigForNextTranslate(content, extension);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to include Intlayer next-translate compat plugin`);
			} else logger(`${v} ${colorizePath(file)} already includes @intlayer/next-translate`);
		} else if (!content.includes("next-intlayer")) {
			const updatedContent = updateNextConfig(content, extension);
			await writeFileToRoot(rootDir, file, updatedContent);
			logger(`${v} Updated ${colorizePath(file)} to include Intlayer plugin`);
		}
		break;
	}
	if (isNextJsProject) await setupNextCompilerBabelConfig({
		rootDir,
		packageManager,
		allDeps,
		skipInstall: Boolean(options?.noInstallPackages)
	});
	for (const file of [
		"astro.config.mjs",
		"astro.config.js",
		"astro.config.ts",
		"astro.config.cjs"
	]) if (await exists(rootDir, file)) {
		hasAliasConfiguration = true;
		if (file.startsWith("astro.config.")) {
			const content = await readFileFromRoot(rootDir, file);
			if (!content.includes("astro-intlayer")) {
				const extension = file.split(".").pop();
				const updatedContent = updateAstroConfig(content, extension);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to include Intlayer integration`);
			}
		}
		break;
	}
	for (const file of ["nuxt.config.js", "nuxt.config.ts"]) if (await exists(rootDir, file)) {
		hasAliasConfiguration = true;
		const content = await readFileFromRoot(rootDir, file);
		if (allDeps["@nuxtjs/i18n"]) {
			if (!content.includes("@intlayer/nuxtjs-i18n")) {
				const updatedContent = updateNuxtConfigForNuxtjsI18n(content);
				await writeFileToRoot(rootDir, file, updatedContent);
				logger(`${v} Updated ${colorizePath(file)} to include @intlayer/nuxtjs-i18n module`);
			} else logger(`${v} ${colorizePath(file)} already includes @intlayer/nuxtjs-i18n`);
		} else if (!content.includes("nuxt-intlayer")) {
			const updatedContent = updateNuxtConfig(content);
			await writeFileToRoot(rootDir, file, updatedContent);
			logger(`${v} Updated ${colorizePath(file)} to include Intlayer module`);
		}
		break;
	}
	if (Boolean(allDeps["react-native"] || allDeps.expo)) {
		hasAliasConfiguration = true;
		const metroConfigs = [
			"metro.config.js",
			"metro.config.cjs",
			"metro.config.mjs",
			"metro.config.ts"
		];
		let metroConfigFile;
		for (const file of metroConfigs) if (await exists(rootDir, file)) {
			metroConfigFile = file;
			break;
		}
		if (metroConfigFile) {
			const content = await readFileFromRoot(rootDir, metroConfigFile);
			if (content.includes("react-native-intlayer")) logger(`${v} ${colorizePath(metroConfigFile)} already includes the Intlayer Metro plugin`);
			else {
				const extension = metroConfigFile.split(".").pop();
				const updatedContent = updateMetroConfig(content, extension);
				if (updatedContent !== content) {
					await writeFileToRoot(rootDir, metroConfigFile, updatedContent);
					logger(`${v} Updated ${colorizePath(metroConfigFile)} to include the Intlayer Metro plugin`);
				} else logger(`${x} Could not automatically update ${colorizePath(metroConfigFile)}. Wrap your exported config with ${colorize("configMetroIntlayer", ANSIColors.MAGENTA)}: ${colorizePath(DocumentationRouter.ReactNativeAndExpo)}`, { level: "warn" });
			}
		} else {
			const newMetroConfigFile = "metro.config.js";
			await writeFileToRoot(rootDir, newMetroConfigFile, getMetroConfigTemplate(Boolean(allDeps.expo)));
			logger(`${v} Created ${colorizePath(newMetroConfigFile)} with the Intlayer Metro plugin`);
		}
	}
	const backendIntlayerPackages = [
		"express-intlayer",
		"fastify-intlayer",
		"adonis-intlayer",
		"hono-intlayer"
	];
	const devScript = packageJson.scripts?.dev;
	let newDevScript;
	if (devScript && (isNextJsProject && allDeps.next && isVersionAtLeast(allDeps.next, 16) || backendIntlayerPackages.some((pkg) => allDeps[pkg])) && !devScript.includes("intlayer watch")) newDevScript = `intlayer watch --with '${devScript}'`;
	if (newDevScript) {
		packageJson.scripts.dev = newDevScript;
		await writeFileToRoot(rootDir, packageJsonPath, JSON.stringify(packageJson, null, 2));
		logger(`${v} Updated ${colorizePath("package.json")} dev script to run intlayer watch`);
	}
	for (const file of [
		"webpack.config.js",
		"webpack.config.ts",
		"webpack.config.mjs",
		"webpack.config.cjs"
	]) if (await exists(rootDir, file)) {
		hasAliasConfiguration = true;
		logger(`${v} Found ${colorizePath(file)}. Make sure to configure aliases manually or use the Intlayer Webpack plugin.`);
		break;
	}
	if ([
		"express",
		"fastify",
		"@adonisjs/core",
		"hono",
		...backendIntlayerPackages
	].some((pkg) => allDeps[pkg])) hasAliasConfiguration = true;
	if (!hasAliasConfiguration) {
		const configuration = getConfiguration({ baseDir: rootDir });
		const aliases = getAlias({ configuration });
		if (hasTsConfig && tsConfigFiles.length > 0) {
			const tsConfigPath = tsConfigFiles.find((file) => file === "tsconfig.json") || tsConfigFiles[0];
			if (tsConfigPath) {
				const tsConfigContent = await readFileFromRoot(rootDir, tsConfigPath);
				const config = parseJSONWithComments(tsConfigContent);
				config.compilerOptions ??= {};
				config.compilerOptions.paths ??= {};
				let updated = false;
				Object.entries(aliases).forEach(([alias, path]) => {
					if (!config.compilerOptions.paths[alias]) {
						config.compilerOptions.paths[alias] = [path];
						updated = true;
					}
				});
				if (updated) {
					await writeFileToRoot(rootDir, tsConfigPath, JSON.stringify(config, null, 2));
					logger(`${v} Updated ${colorizePath(tsConfigPath)} to include Intlayer aliases`);
				}
			}
		} else {
			const jsConfigPath = "jsconfig.json";
			if (await exists(rootDir, jsConfigPath)) {
				const jsConfigContent = await readFileFromRoot(rootDir, jsConfigPath);
				const config = parseJSONWithComments(jsConfigContent);
				config.compilerOptions ??= {};
				config.compilerOptions.paths ??= {};
				let updated = false;
				Object.entries(aliases).forEach(([alias, path]) => {
					if (!config.compilerOptions.paths[alias]) {
						config.compilerOptions.paths[alias] = [path];
						updated = true;
					}
				});
				if (updated) {
					await writeFileToRoot(rootDir, jsConfigPath, JSON.stringify(config, null, 2));
					logger(`${v} Updated ${colorizePath(jsConfigPath)} to include Intlayer aliases`);
				}
			} else {
				packageJson.imports ??= {};
				let updated = false;
				Object.entries(aliases).forEach(([alias, path]) => {
					const importAlias = alias.replace("@", "#");
					const importPath = path.startsWith(".") ? path : `./${path}`;
					if (!packageJson.imports[importAlias]) {
						packageJson.imports[importAlias] = importPath;
						updated = true;
					}
				});
				if (updated) {
					await writeFileToRoot(rootDir, packageJsonPath, JSON.stringify(packageJson, null, 2));
					logger(`${v} Updated ${colorizePath(packageJsonPath)} to include Intlayer imports`);
				}
			}
		}
	}
	const hasCompatLibrary = Boolean(compatSyncConfig || compatVitePluginConfig);
	if (!options?.noFrameworkSetup && hasCompatLibrary) logger(`${v} Compat i18n library detected — keeping the app structure untouched (no route restructure, no provider injection)`);
	if (!options?.noFrameworkSetup && !hasCompatLibrary) try {
		const routingMode = options?.routingMode ?? getConfiguration({ baseDir: rootDir }).routing.mode;
		await setupFramework({
			rootDir,
			allDeps,
			packageManager,
			useTypeScript: hasTsConfig,
			routingMode
		});
	} catch {
		logger(`${x} Framework-specific scaffolding failed. Your existing files were left untouched; follow the documentation to finish the setup.`, { level: "warn" });
	}
	logger(`${v} ${colorize("Intlayer init setup complete.", ANSIColors.GREEN)}`);
	logger([
		colorize("Next →", ANSIColors.MAGENTA),
		colorize(`Follow the instructions in the documentation to complete the setup:`, ANSIColors.GREY_LIGHT),
		colorizePath(guideUrl)
	]);
	logger([
		colorize("Intlayer LSP →", ANSIColors.MAGENTA),
		colorize("The Intlayer Language Server is available via the `@intlayer/lsp` package. It lets IDEs and AI agents jump from `useIntlayer`/`getIntlayer` keys to their `.content` files.", ANSIColors.GREY_LIGHT),
		colorize("Ask the user which platform/editor they want to enable it on (VS Code, Cursor, Windsurf, Zed, Neovim, Claude Code, …), then follow the matching setup instructions:", ANSIColors.GREY_LIGHT),
		colorizePath(DocumentationRouter.LSP)
	]);
};

//#endregion
export { getDefaultApplicationURL, initIntlayer };
//# sourceMappingURL=index.mjs.map