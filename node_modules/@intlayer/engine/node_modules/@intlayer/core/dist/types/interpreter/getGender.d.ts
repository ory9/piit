import { Gender, GenderContentStates } from "../transpiler/gender/gender.js";
import "../transpiler/index.js";
//#region src/interpreter/getGender.d.ts
type GenderEntry = Gender | 'm' | 'f';
/**
 * Allow to pick a content based on a gender.
 *
 * Usage:
 *
 * ```ts
 * const content = getGender({
 *  'true': 'The gender is validated',
 *  'false': 'The gender is not validated',
 * }, true);
 * // 'The gender is validated'
 * ```
 *
 * The last key provided will be used as the fallback value.
 *
 * ```ts
 * const content = getGender({
 *  'false': 'The gender is not validated',
 *  'true': 'The gender is validated',
 * }, undefined);
 * // 'The gender is validated'
 */
declare const getGender: <const Content>(genderContent: GenderContentStates<Content>, gender: GenderEntry) => Content;
//#endregion
export { getGender };
//# sourceMappingURL=getGender.d.ts.map