Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_core_AnalyticsClient = require('./AnalyticsClient.cjs');
const require_core_globalManager = require('./globalManager.cjs');
let _intlayer_config_built = require("@intlayer/config/built");

//#region src/core/initAnalyticsClient.ts
/**
* Initializes the Intlayer analytics client singleton.
*
* Behavior comes from the `analytics` config block (`flushInterval`,
* `sampleRate`); attribution reuses the existing `editor` config
* (`editor.backendURL` for the endpoint, `editor.clientId` for the project).
*
* Safe to call multiple times: returns the existing client and increments a
* `window`-shared reference count so nested providers (or duplicated module
* instances) don't stop the client prematurely.
*
* @returns The started analytics client singleton.
*/
const initAnalyticsClient = () => {
	require_core_globalManager.incrementAnalyticsClientRefCount();
	const existing = require_core_globalManager.getGlobalAnalyticsClient();
	if (existing) return existing;
	const client = require_core_AnalyticsClient.createAnalyticsClient({
		backendURL: _intlayer_config_built.editor?.backendURL ?? "",
		clientId: _intlayer_config_built.editor?.clientId,
		flushInterval: _intlayer_config_built.analytics?.flushInterval ?? 2e4,
		maxBufferSize: 500,
		sampleRate: _intlayer_config_built.analytics?.sampleRate ?? 1
	});
	require_core_globalManager.setGlobalAnalyticsClient(client);
	client.start();
	return client;
};
/**
* Decrements the shared reference count and stops the global analytics client
* only when the last provider unmounts (flushing any remaining events).
*
* Callers must pair each `stopAnalyticsClient` with a prior
* `initAnalyticsClient` — never call it when init was skipped or cancelled.
*/
const stopAnalyticsClient = () => {
	if (require_core_globalManager.decrementAnalyticsClientRefCount() > 0) return;
	require_core_globalManager.getGlobalAnalyticsClient()?.stop();
	require_core_globalManager.setGlobalAnalyticsClient(null);
};

//#endregion
exports.initAnalyticsClient = initAnalyticsClient;
exports.stopAnalyticsClient = stopAnalyticsClient;
//# sourceMappingURL=initAnalyticsClient.cjs.map