import { assignVariant } from "./core/abTesting.mjs";
import { createEventBuffer } from "./core/eventBuffer.mjs";
import { isSampledIn } from "./core/sampling.mjs";
import { getSessionId } from "./core/session.mjs";
import { sendEvents } from "./core/transport.mjs";
import { DEFAULT_FLUSH_INTERVAL, DEFAULT_MAX_BUFFER_SIZE, DEFAULT_SAMPLE_RATE, MAX_EVENTS_PER_SEND, createAnalyticsClient } from "./core/AnalyticsClient.mjs";
import { decrementAnalyticsClientRefCount, getGlobalAnalyticsClient, incrementAnalyticsClientRefCount, setGlobalAnalyticsClient } from "./core/globalManager.mjs";
import { initAnalyticsClient, stopAnalyticsClient } from "./core/initAnalyticsClient.mjs";
import { buildContentExposure, serializeKeyPath } from "./events/contentExposure.mjs";

export { DEFAULT_FLUSH_INTERVAL, DEFAULT_MAX_BUFFER_SIZE, DEFAULT_SAMPLE_RATE, MAX_EVENTS_PER_SEND, assignVariant, buildContentExposure, createAnalyticsClient, createEventBuffer, decrementAnalyticsClientRefCount, getGlobalAnalyticsClient, getSessionId, incrementAnalyticsClientRefCount, initAnalyticsClient, isSampledIn, sendEvents, serializeKeyPath, setGlobalAnalyticsClient, stopAnalyticsClient };