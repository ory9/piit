import { AnalyticsClient } from "./AnalyticsClient.js";
//#region src/core/globalManager.d.ts
/**
 * Returns the global analytics client, or `null` in SSR or before init.
 */
declare const getGlobalAnalyticsClient: () => AnalyticsClient | null;
/**
 * Sets (or clears) the global analytics client singleton.
 *
 * @param client - The client to store globally, or `null` to clear it.
 */
declare const setGlobalAnalyticsClient: (client: AnalyticsClient | null) => void;
/**
 * Increments the shared provider reference count.
 *
 * @returns The count after incrementing (`0` in SSR, where nothing is stored).
 */
declare const incrementAnalyticsClientRefCount: () => number;
/**
 * Decrements the shared provider reference count, clamping at zero.
 *
 * @returns The count after decrementing (`0` in SSR, where nothing is stored).
 */
declare const decrementAnalyticsClientRefCount: () => number;
//#endregion
export { decrementAnalyticsClientRefCount, getGlobalAnalyticsClient, incrementAnalyticsClientRefCount, setGlobalAnalyticsClient };
//# sourceMappingURL=globalManager.d.ts.map