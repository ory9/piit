import { DictionaryStatus } from "@intlayer/engine/build";
//#region src/push/pullLog.d.ts
type PullStatus = {
  dictionaryKey: string;
  status: DictionaryStatus | 'pending' | 'fetching';
  errorMessage?: string;
};
declare class PullLogger {
  private statuses;
  private spinnerTimer;
  private spinnerIndex;
  private renderedLines;
  private readonly spinnerFrames;
  private isFinished;
  private readonly prefix;
  private lastRenderedState;
  constructor();
  update(newStatuses: PullStatus[]): void;
  finish(): void;
  private startSpinner;
  private stopSpinner;
  private render;
  private computeProgress;
}
//#endregion
export { PullLogger, PullStatus };
//# sourceMappingURL=pullLog.d.ts.map