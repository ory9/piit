import * as ANSIColors from "@intlayer/config/colors";
import { colorize, colorizeNumber, spinnerFrames } from "@intlayer/config/logger";

//#region src/reviewDoc/reviewDocProgress.ts
/** Refresh rate of the spinner, in milliseconds. */
const SPINNER_INTERVAL_MS = 100;
/**
* Single-line progress indicator used while the documents are compared with
* their translations.
*
* The line is rewritten in place on an interactive terminal, then erased so the
* final synthesis is printed on a clean line. On a non-interactive output (a
* file, a CI log) a single static line is printed instead of the animation, to
* avoid flooding the log with escape sequences.
*
* @param totalCount - Number of document/locale pairs to process.
* @returns The progress logger handles.
*/
const createReviewProgressLogger = (totalCount) => {
	const isInteractive = Boolean(process.stdout.isTTY);
	let spinnerTimer = null;
	let spinnerIndex = 0;
	let processedCount = 0;
	let isStarted = false;
	const render = () => {
		const frame = spinnerFrames[spinnerIndex] ?? "";
		process.stdout.write(`\r\x1b[2K${colorize(frame, ANSIColors.BLUE)} Processing ${colorizeNumber(processedCount)}/${colorizeNumber(totalCount)}…`);
	};
	const start = () => {
		isStarted = true;
		if (!isInteractive) {
			process.stdout.write(`Processing ${colorizeNumber(totalCount)} document/locale pair(s)…\n`);
			return;
		}
		if (spinnerTimer) return;
		render();
		spinnerTimer = setInterval(() => {
			spinnerIndex = (spinnerIndex + 1) % spinnerFrames.length;
			render();
		}, SPINNER_INTERVAL_MS);
	};
	const increment = () => {
		processedCount += 1;
		if (isStarted && isInteractive) render();
	};
	const stop = () => {
		if (spinnerTimer) {
			clearInterval(spinnerTimer);
			spinnerTimer = null;
		}
		if (isStarted && isInteractive) process.stdout.write("\r\x1B[2K");
		isStarted = false;
	};
	return {
		start,
		increment,
		stop
	};
};

//#endregion
export { createReviewProgressLogger };
//# sourceMappingURL=reviewDocProgress.mjs.map