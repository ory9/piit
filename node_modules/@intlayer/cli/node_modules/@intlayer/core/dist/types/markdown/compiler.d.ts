import { CompileOptions, MarkdownContext, MarkdownOptions, MarkdownRuntime } from "./types.js";
//#region src/markdown/compiler.d.ts
type FootnoteDef = {
  footnote: string;
  identifier: string;
};
/** Link/image reference definitions (`[id]: target "title"`) collected while parsing. */
type MarkdownReferences = Record<string, {
  target: string;
  title?: string;
}>;
type ParsedMarkdown = {
  ast: any[];
  footnotes: FootnoteDef[];
  /**
   * Reference definitions gathered during parsing. They must be carried over to
   * the render stage, since `refLink` / `refImage` nodes only store the
   * reference id and resolve their target at render time.
   */
  references: MarkdownReferences;
  inline: boolean;
};
declare const parseMarkdown: (markdown: string, ctx: MarkdownContext<any>, options?: MarkdownOptions) => ParsedMarkdown;
declare const renderMarkdownAst: (parsed: ParsedMarkdown, ctx: MarkdownContext<any>, options?: MarkdownOptions) => unknown;
declare const compile: (markdown: string, ctx: MarkdownContext<any>, options?: MarkdownOptions) => unknown;
declare const createCompiler: (ctx: MarkdownContext<any>) => (markdown: string, options?: MarkdownOptions) => unknown;
declare const compileWithOptions: (markdown: string, runtime: MarkdownRuntime, options?: CompileOptions<any>) => unknown;
//#endregion
export { MarkdownReferences, ParsedMarkdown, compile, compileWithOptions, createCompiler, parseMarkdown, renderMarkdownAst };
//# sourceMappingURL=compiler.d.ts.map